% === Begin Label Data ===
% ID: 402
% 难度星级: 
% 标签: 
% 备注: 
% 组卷引用次数: 0
% === End  Label Data ===

\begin{problem}{2005}{G}{北京卷（理）}{14}{数列}
已知 $ n $ 次多项式 $ P_n(x)=a_0x^n+a_1x^{n-1}+\cdots+a_{n-1}x+a_n $．  
如果在一种算法中，计算 $ x_0^k $（$ k=2,3,4,\cdots,n $）的值需要 $ k-1 $ 次乘法，计算 $ P_3(x_0) $ 的值共需要 $ 9 $ 次运算（$ 6 $ 次乘法，$ 3 $ 次加法），那么计算 $ P_{10}(x_0) $ 的值共需要\underline{\hspace{4em}}次运算．  
下面给出一种减少运算次数的算法：$ P_0(x)=a_0 $，$ P_{k+1}(x)=xP_k(x)+a_{k+1} $（$ k=0,1,2,\cdots,n-1 $）．利用该算法，计算 $ P_3(x_0) $ 的值需要 $ 6 $ 次运算，计算 $ P_{10}(x_0) $ 的值需要\underline{\hspace{4em}}次运算．
\end{problem}

\begin{answer}

\end{answer}

\begin{solutions}

\end{solutions}