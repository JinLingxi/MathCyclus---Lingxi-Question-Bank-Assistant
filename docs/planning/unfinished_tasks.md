# 当前未完成事项清单

> 说明：这是当前真正还没做完、还需要继续推进的事项索引。  
> `docs/planning/题库数据库重构实施规划.md` 是主规划，`docs/planning/题库重构参考沉淀.md` 是外部参考沉淀；本文件只负责收敛“下一步还要做什么”。
> 最近同步：2026-09-11。当前主链路无阻断项；PDF 导入已完成 P0-P3 可用首版，数据清洗已进入“试卷命名标准化 + 重复题治理”阶段。

## P0：主链路稳定性复核

- `question_bank_app.py`、`services/*.py`、`scripts/*.py` 语法检查此前已通过。
- 2026-09-11 完整 `python scripts/release_readiness.py` 已通过，`failed=0`、`warnings=2`。
- 最新清洗状态：SQLite 与旧 TeX 均为 979 题，`tex_only=0`、`sqlite_only=0`、`blocking_mismatches=0`。
- 当前剩余 warning 主要是旧 TeX 与 SQLite 的非阻塞字段差异、难度/标签/答案解析补全等后续任务。
- 入口策略已修正：`全局浏览与编辑` 继续走旧 TeX 浏览；`SQLite预览` 单独承载 SQLite 题目预览与编辑试用。

## P1：当前最重要的功能补齐

- [x] 试卷标准名录、历史别名、匹配候选和人工确认记录的数据库结构已加入 schema version 3。
- [x] 单题、批量、同卷录入已接入试卷名录匹配；精确命中自动采用，相似/新增需要人工确认。
- [x] 工具箱已增加标准名录管理与待审核试卷列表，支持人工新增别名、废弃标准名和查看审核记录。
- [x] 批量录入中出现多个不同试卷时，按试卷分组显示确认面板；每组确认一次并继承到该组草稿。

- 继续收口 `录入问题` 结构化 UI：单题、批量试题、同卷试题、同书试题仍需按实机反馈优化。
- 入口策略改为“旧名称入口 + 工具箱偏好切换”：侧边栏默认只展示 `录入新题` 与 `全局浏览与编辑`，默认打开旧 TeX 页面；SQLite 新录入/预览通过工具箱切换本机偏好。
- 巩固 AI/OCR 草稿导入：识别填入、草稿审核、确认入库、修订记录、图片草稿登记都要保持可回溯。
- [x] PDF 导入草稿链路 P0/P1 已完成：已补 `data/imports/pdf_jobs/` 私有目录、`.gitignore` 边界和 `services/pdf_import_service.py`。
- [x] PyMuPDF 已支持逐页 PNG 渲染、文本提取，并生成 `job_manifest.json` 与 `draft_payload.json`；烟测确认不写正式数据库。
- [x] PDF P2 已支持按题号行生成候选、跨页合并、卷头文本保留和无题号整页兜底，并生成 `import_report.md`。
- [x] `scripts/create_pdf_import_job.py` 已提供命令行入口；输出继续只写 job 文件，不直接写正式 `question` 表。
- [x] PDF P3 底层原型曾接入录入页面；根据实机反馈取消独立入口，后续解析能力统一并入同卷/同书录入。
- [x] `scripts/smoke_pdf_import_ui.py` 已用 Streamlit AppTest 覆盖 PDF 模式、页面图片和候选表单，确认不改正式数据库。
- [x] 统一文档录入 P3.1 已落地 PyMuPDF 逐题裁剪、内嵌图/矢量图提取、图片自动绑定、用途调整、跨题重绑和移除。
- [x] 真实 `2026年全国Ⅰ卷.pdf` 已稳定切分为连续 1-19 题，考试须知和小问不再误判为主问题，第 15 题矢量图仅绑定一次。
- [x] AI 精修已改为逐题调用，只发送当前题裁剪图与提取文本，不发送整份 PDF。
- [x] 批量 AI 队列支持进度持久化、分段处理、成功项防重复、失败重试、意外中断恢复和最近任务恢复。
- [x] 批量 AI 范围已改为全部未完成或明确起止题号，知识板块受固定选项约束并在逐题卡片中使用多选下拉。
- [x] 批量/同卷/同书专属界面已重排为上方三列操作区和下方两列逐题校订区；元数据常显，源码与图片折叠编辑。
- 下一步继续 P3.2：安装并实测 OpenCV 扫描页区域检测，增加可拖拽裁剪框与图片合并；之后收口整批校验和正式入库摘要。
- 继续按页面反馈处理 SQLite 预览、专题收录、组卷篮、检索模式的局部 UI 问题，但不再改变现有核心数据流。

## P2：数据模型与关系补强

- 补强题目、试卷、教材、专题、资源、修订记录之间的双向关系展示与编辑入口。
- 完善题目图片、题内多图、资源命名、排序、用途、回溯和导出规范。
- 数据清洗已启动：新增 `scripts/audit_data_cleanup_candidates.py` 和 `docs/planning/data_cleanup_and_naming_plan.md`。
- 第一轮人工确认删除已完成：删除 `Q000114`、`Q000833`、`Q000646`，保留 `Q000113`、`Q000271`、`Q000656`，并同步清理删除 `Q000646` 后留下的空试卷 `Pb59f62ea27`。
- 2016 浙江卷文理共用题已按人工确认拆分：`Q000029` 拆为 `Q000894`（文科第 5 题）和 `Q000895`（理科第 5 题），`Q000473` 拆为 `Q000896`（文科第 6 题）和 `Q000897`（理科第 6 题）。
- 江苏卷跨板块重复题已按人工确认处理：保留 `Q000768`，删除 `Q000028`。
- 第二轮弱副本处理已完成：删除 `Q000132`、`Q000150`、`Q000509`、`Q000508`；分别将知识点补到 `Q000362`、`Q000377`、`Q000434`，并将 `Q000508` 的试卷关系、答案、解析合并到 `Q000506`。
- 第二轮题号修正已完成：`Q000157` 保持为 2024 上海卷第 18 题，并将旧文件路径从 `2024-G-上海卷-21-函数.tex` 修正为 `2024-G-上海卷-18-函数.tex`。
- `Q000496` 已按人工确认同步为 2021 上海春考卷第 9 题：旧 TeX 路径、`\begin{problem}` 头、`question` 与 `legacy_question_map` 元数据已同步；同步后题号不一致降为 0。
- 试卷名称标准化已启动：`docs/planning/paper_name_normalization.md` 已重写为可读规范，`scripts/audit_paper_naming_standard.py` 已生成只读审计报告；下一步需要人工确认 72 个文理 track 复核项、20 个自动标准化候选和 4 个可能合并的试卷组。
- 继续清洗旧 TeX 与 SQLite 的 82 条非阻塞字段差异，作为独立数据复核任务处理。

## P3：发布与安装能力

- 源码白名单打包策略已具备脚本和检查项，真实发布前需要继续复核。
- 评估独立启动器或更完整的安装包方案；原则是程序包和用户私有数据包分离。
- 完善用户数据目录迁移、迁移包版本兼容检查和一键恢复体验。
- 继续打磨旧版本数据导入到新 SQLite 结构的迁移入口，让早期用户可安全升级。

## P4：可继续探索但不阻塞主线

- 深化 MinerU / pdf2md 的 PDF 解析能力；当前先以 PyMuPDF / 手动截图兜底，不把 MinerU 作为基础安装必需依赖。
- 继续优化专题收录、组卷篮、全局浏览的操作效率。
- 继续优化检索模式、分页、筛选联动和局部刷新体验。

## 当前建议保留的主文档

- `docs/planning/题库数据库重构实施规划.md`：总规划与阶段路线。
- `docs/planning/题库重构参考沉淀.md`：外部题库系统与 AI 录入参考。
- `docs/planning/ai_ocr_draft_import_flow.md`：AI/OCR 草稿流程细化。
- `docs/planning/pdf_import_pipeline.md`：PDF 导入路线补充。
- `docs/planning/data_cleanup_and_naming_plan.md`：旧题库数据清洗、重复题复核、试卷命名和文理标注规范。
- `docs/planning/paper_name_normalization.md`：试卷命名标准化规则、文理 track 规则、重复题治理原则。
- `docs/planning/release_file_checklist.md`：发布前文件边界。
- `docs/planning/local_workspace_and_migration.md`：本地初始化与迁移。
- `docs/planning/version_update_and_installer.md`：源码更新、启动器和安装包边界。
- `docs/planning/database_schema_notes.md`：当前 SQLite schema 表职责说明。

## 可直接清理的中间文件

- `tatus --short`：此前误生成的命令输出文件，如仍出现在本地可直接删除。
- `reports/` 下本轮检查报告属于本地运行产物，默认被 `.gitignore` 保护，不需要提交。
- 其余备份/缓存文件先保留，除非后续确认它们已经完全没有回滚用途。

## 2026-09-09 旧 TeX 编译入口收敛

- [x] 将根目录 `main.tex` 定位为旧 TeX 兼容编译模板，不再视为 SQLite 新题库主入口。
- [x] 将工具箱“更新板块题目索引”更名为“旧 TeX 章节汇总索引”，明确其只生成 `content_*.tex`。
- [x] 将旧 TeX 章节汇总索引正式收拢到“旧 TeX 兼容工具”内部，并从主工具网格移除。
- [x] 将旧 TeX 的 TikZ 辅助副本修复正式收拢到“旧 TeX 兼容工具”，并增加批量写入确认。
- [x] SQLite → 兼容 `problem` 命令的 TeX 导出器已完成，支持单题、筛选结果、试卷、专题和教材范围。
- [ ] 导出器稳定后，将 `main.tex` 归档到 `templates/legacy/` 或 `legacy_tex/`，并同步 README、发布白名单和打包脚本。

## 2026-09-09 工具箱旧功能收敛

- [x] 删除主工具箱“纠正选择题选项格式”卡片及其旧 TeX 全库正则批处理函数；新版结构化选项不再依赖该修复器。
- [x] 删除主工具箱独立“标签与属性修改”卡片及不可达页面；旧 TeX 单题继续在全局浏览中修改文件信息，SQLite 单题继续在数据库题卡中修改题目与标签信息。
- [x] 已新增事务化、可预览差异、失败自动回滚的 SQLite 批量维护工具，不复用旧 TeX 文件重命名页面。
- [x] 主工具箱收敛为 8 个入口，并重排为两行四列。

