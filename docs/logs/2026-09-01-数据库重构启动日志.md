# 2026-09-01 数据库重构启动日志

## 本轮目标

启动 MathCyclus 题库数据库化重构的第一批安全任务：

- 不改现有 Streamlit 页面功能；
- 不修改现有 `.tex` 题库文件；
- 建立未来数据库、扫描、迁移、文件管理的基础骨架；
- 用只读扫描和 dry-run 迁移验证方向。

## 已完成

### 1. 规划文档

新增：

- `题库重构参考沉淀.md`
- `题库数据库重构实施规划.md`
- `docs/planning/question_id_rules.md`
- `docs/planning/database_schema_notes.md`

这些文档分别记录：

- 朋友题库系统给我们的结构启发；
- SQLite 主数据库 + TeX 兼容导出的长期实施路线；
- 题目稳定 ID 规则；
- 第一版数据库表结构说明。

### 2. 目录骨架

新增目录：

```text
assets/
assets/questions/
data/
data/indexes/
db/
db/migrations/
db/seed/
docs/
docs/archive/
docs/logs/
docs/planning/
exports/
reports/
scripts/
templates/
```

所有目录均放置 `.gitkeep`，方便 Git 保留骨架。

### 3. Git 忽略规则

调整 `.gitignore`：

- 继续忽略实际 SQLite 数据库；
- 继续忽略 `data/` 中生成的数据内容；
- 保留 `data/.gitkeep` 和 `data/indexes/.gitkeep`；
- 继续忽略 `exports/` 中用户生成导出物；
- 继续忽略 `assets/questions/` 中未来题目图片资源；
- 不再忽略两份重构规划文档。

### 4. 数据库 schema

新增：

- `db/schema.sql`

第一版包含 16 张表：

- `question_type`
- `question`
- `question_analysis`
- `paper`
- `paper_question`
- `book`
- `book_section`
- `book_exercise_question`
- `topic_module`
- `topic`
- `topic_question`
- `question_asset`
- `question_revision`
- `import_batch`
- `import_report_item`
- `legacy_question_map`

### 5. 只读扫描脚本

新增：

- `scripts/scan_tex_library.py`

该脚本只读扫描 `chapters/`，不会修改任何题库文件。

已生成：

- `reports/tex_scan_20260901_initial.md`

生成但被 `.gitignore` 忽略：

- `reports/tex_scan_20260901_initial.csv`
- `reports/tex_scan_20260901_initial.summary.json`

### 6. Dry-run 迁移脚本

新增：

- `scripts/migrate_tex_to_db_dry_run.py`

该脚本生成预览 SQLite，不修改任何 `.tex` 文件。

已生成但被 `.gitignore` 忽略：

- `data/mathcyclus_preview_20260901_initial.sqlite3`

已生成：

- `reports/tex_to_db_dry_run_20260901_initial.md`

### 7. 数据库服务层

新增：

- `services/database_service.py`
- `services/question_db_service.py`

当前只提供基础连接和只读查询能力，尚未接入页面。

### 8. 反向 TeX 导出原型

新增：

- `services/export_service.py`
- `scripts/export_db_to_tex.py`

已验证：

- 可从预览 SQLite 读取题目；
- 可导出单题 `.tex`；
- 可批量导出前 N 题 `.tex`；
- 导出位置为 `exports/`，不会覆盖 `chapters/` 原题库；
- 导出的 `.tex` 保持当前旧题库的 `Label Data + problem + answer + solutions` 结构。

已生成：

- `reports/db_to_tex_export_20260901_single.md`
- `reports/db_to_tex_export_20260901_sample.md`

导出的实际 `.tex` 文件在 `exports/` 下，被 `.gitignore` 忽略。

## 扫描结果摘要

扫描正式题目：

- 题目 TeX 文件数：893
- 状态 `ok`：892
- 状态 `warning`：1
- 状态 `needs_review`：0
- 缺失元数据块：0
- 缺失旧 ID：0
- 缺失 problem 环境：0
- 重复旧 ID：0
- 含 TikZ 题目：64
- 含 `includegraphics` 题目：1
- 图片引用缺失题目：1
- 已排除 `相关图/` 辅助 TeX：85

唯一警告：

- `chapters/立体几何/2021/2021-G-新高考I卷-12-立体几何.tex`
- 警告：`missing_image_refs`

## Dry-run 迁移结果

预览迁移：

- `question`：893 条
- `question_analysis`：893 条
- `paper`：301 条
- `paper_question`：893 条
- `legacy_question_map`：893 条

样例：

- `Q000001` ← `chapters/三角函数/2004/2004-G-天津卷（理）-12-三角函数.tex`
- 旧 ID：`233`
- 试卷：`2004 天津卷（理）`

## 反向导出验证结果

已用预览库验证：

- `Q000001` 可导出为单题 TeX；
- 前 5 题可批量导出；
- 输出目录位于 `exports/db_to_tex_20260901_single/` 和 `exports/db_to_tex_20260901_sample/`；
- 导出产物不会进入 Git。

## 当前未处理事项

- `question_bank_app.py` 尚未接入 SQLite。
- 数据库目前只是预览库，不是正式主库。
- 图片资源尚未迁移到 `assets/questions/<question_id>/`。
- `Paper` 的标准化命名还比较初步，后面需要专门做试卷名称映射表。
- 教材库和专题库只是 schema 层预留，尚未实现页面和服务。
- `reports/*.csv` 和预览 SQLite 被忽略，只作为本地检查文件。
- 当前反向导出原型还没有处理图片复制、图片占位符和路径重写。

## 下一步建议

下一轮建议做：

1. 检查并修复唯一图片引用缺失题目。
2. 建立正式的试卷名称标准化规则文档。
3. 给反向导出增加图片资源复制和路径重写。
4. 建立 `paper_name_mapping` 草案。
5. 再考虑把全局浏览的一小部分读取逻辑切换到预览库，先做只读模式，不动保存逻辑。

---

## 第二轮补充进展

### 1. 图片引用缺失检查

已定位唯一图片引用缺失题目：

- `chapters/立体几何/2021/2021-G-新高考I卷-12-立体几何.tex`
- 旧 ID：`411`
- 预览 ID：`Q000696`

缺失引用：

- `images/2021 新高考I 卷 12答案1.png`
- `images/2021 新高考I 卷 12答案2.png`
- `images/2021 新高考I 卷 12答案3.png`

已新增报告：

- `reports/missing_image_refs_20260901.md`

当前没有修改原题库文件。

### 2. 试卷名称清单

已从预览数据库生成：

- `reports/paper_name_inventory_20260901.md`

生成但被忽略：

- `reports/paper_name_inventory_20260901.csv`

当前预览库中识别到 301 条试卷记录。

### 3. 试卷名称标准化规则

已新增：

- `docs/planning/paper_name_normalization.md`

核心原则：

- 保留原始 `source_name`；
- 另设标准 `paper_name`；
- 文科、理科、新高考写入 `track`；
- 不自动合并含义可能不同的试卷名；
- 后续用 `db/seed/paper_name_mapping.csv` 人工维护映射。

### 4. 试卷名称映射草案

已新增：

- `scripts/generate_paper_name_mapping.py`
- `db/seed/paper_name_mapping.csv`

当前映射草案包含 301 条试卷记录，全部标记为 `pending`，表示后续仍需人工审查。

### 5. 图片导出检测增强

已增强：

- `services/export_service.py`
- `scripts/export_db_to_tex.py`

当前导出器可以：

- 检测 `\includegraphics` 引用；
- 在报告中列出图片是否缺失；
- 启用 `--copy-graphics` 时复制可找到的图片并重写路径；
- 对缺失图片保持原引用并写入报告，不静默失败。

已用真实缺图题验证：

- 新 ID：`Q000696`
- 旧 ID：`411`
- 图片引用数：3
- 图片缺失数：3
