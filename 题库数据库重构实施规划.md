# 题库数据库重构实施规划

> 记录时间：2026-09-01  
> 适用项目：MathCyclus 高中数学题库  
> 目标：把当前以 `.tex` 文件为核心的题库，逐步升级为“SQLite 主数据库 + TeX 兼容导出 + 图片资源管理 + 试卷/教材/专题双向关系 + AI 草稿录入”的结构化题库系统。  
> 原则：先设计，后迁移；先兼容，后替换；先保证现有功能稳定，再扩展数据库能力。

---

## 1. 已确认的关键决策

### 1.1 主存储方向

未来接受：

- 使用 `SQLite` 作为主数据库。
- 当前 `.tex` 文件全部保留。
- `.tex` 文件不再长期承担唯一主存储职责，而是作为：
  - 迁移来源；
  - 人工可读备份；
  - Git 可追踪文本格式；
  - 批量导出目标；
  - 与现有讲义/试卷排版流程兼容的中间格式。

### 1.2 文件保留与反向导出

现有 `chapters/.../*.tex` 不删除。未来应保留一个从数据库批量导出 `.tex` 文件的入口，至少支持：

- 按知识板块导出；
- 按年份导出；
- 按试卷导出；
- 按专题导出；
- 按教材章节导出；
- 按当前旧题库目录结构导出。

这能保证即使数据库成为主存储，仍然不会失去当前 LaTeX 文件式工作流。

### 1.3 稳定题目 ID

未来每道题必须有稳定 `question_id`。所有关系都挂到这个 ID：

- 试卷关系；
- 教材关系；
- 专题关系；
- 图片附件；
- 修订记录；
- AI/OCR 导入报告；
- 组卷引用统计；
- 前端浏览、搜索、编辑、导出。

不要再让文件路径、文件名、题号或题源承担题目唯一身份。

### 1.4 TeX 与 `problem` 命令兼容

未来数据库中保存结构化字段，同时必须能生成兼容讲义系统的 `problem` 命令或当前题库环境格式。

基本策略：

- 数据库保存拆分字段。
- 前端展示 TeX 源码与右侧渲染。
- 导出器负责把结构化字段转换成目标 TeX 格式。
- 如果未来 `problem` 命令增加字段，只改导出器，不大规模改数据库。

### 1.5 图片支持范围

未来需要支持：

- 题干图片；
- 答案图片；
- 解析图片；
- 原始扫描图；
- 可能的辅助图、缩略图、源 PDF 截图。

图片必须作为题目资源挂接到 `question_id`，不能只靠相对路径散落在 `.tex` 文件旁边。

### 1.6 教材库结构

教材主体结构采用：

> 书籍 → 章节 → 小节 → 页码 → 栏目 → 题号 → 小题

其中“栏目”必须预留，因为教材中可能存在：

- 例题；
- 练习；
- 习题；
- 复习题；
- 探究；
- 思考；
- 阅读材料；
- 拓展；
- 单元检测；
- 项目学习；
- 特殊教材自定义栏目。

### 1.7 试卷库结构

试卷需要区分：

- 年份；
- 卷别；
- 文科/理科/新高考；
- 试卷名称；
- 题号；
- 小题。

建议把“题号、小题”放在 `PaperQuestion` 关系中，而不是放在 `Question` 本体中。

### 1.8 专题结构

专题系统暂定：

> 大专题 → 小专题 → 分组 → 题目

该设计未来可以扩展。关键是专题内的排序、分组、备注必须放在关系表中，不污染题目本体。

### 1.9 修订记录与人类可读日志

未来需要两层日志：

1. 数据库正式修订记录：
   - 存在 `QuestionRevision` 表；
   - 记录人工修改、AI 修改、OCR 导入、批量规范化；
   - 用于回滚、审计和定位问题。

2. 人类可读 Markdown 日志：
   - 记录大版本迁移、数据库结构变化、批量导入报告；
   - 推荐放入 `docs/logs/` 或 `logs/`；
   - 文件名带日期，例如 `2026-09-01-数据库结构设计.md`。

### 1.10 AI/OCR 草稿流

AI/OCR 结果不能直接覆盖正式库。推荐流程类似朋友截图中的录入系统：

1. 粘贴或上传原始材料。
2. AI/OCR 解析为结构化草稿。
3. 前端填入题干、答案、解析、考查目的等字段。
4. 用户查看 TeX 源码与右侧渲染。
5. 系统检查 LaTeX、图片引用、字段完整性。
6. 用户确认后写入正式数据库。
7. 写入时生成修订记录和导入报告。

---

## 2. 未来系统的整体架构

推荐架构：

```text
Streamlit / Future H5 Frontend
        |
        v
Application Services
        |
        +-- QuestionService
        +-- PaperService
        +-- BookService
        +-- TopicService
        +-- AssetService
        +-- ImportService
        +-- ExportService
        +-- RevisionService
        |
        v
SQLite Database
        |
        +-- assets/questions/<question_id>/
        +-- exported_tex/
        +-- import_reports/
        +-- backups/
```

当前 Streamlit 可以继续使用，但页面层不应直接处理全部业务逻辑。后续应逐步把逻辑拆到服务层：

- 页面负责展示和交互；
- 服务层负责校验、查询、保存、导出；
- 数据库层负责结构化存储；
- 文件层负责图片、TeX 导出、备份和日志。

---

## 3. 推荐数据库表设计

下面是第一轮重构的建议表结构。真正实现时可以使用 SQLite，字段类型根据 SQLite 习惯调整。

### 3.1 `Question`：题目本体表

用于保存题目的核心内容。

建议字段：

| 字段 | 说明 |
| --- | --- |
| `question_id` | 稳定题目 ID，主键 |
| `question_type_id` | 题型 ID |
| `stem_tex` | 题干 TeX |
| `choices_json` | 选项，可存 A/B/C/D/E 或空 |
| `answer_tex` | 答案 TeX |
| `solution_tex` | 解析 TeX |
| `difficulty` | 难度星级 |
| `tags_json` | 标签列表 |
| `note` | 题目本体备注 |
| `official_flag` | 是否官方 |
| `canonical_tex` | 当前标准化完整 TeX，可缓存生成结果 |
| `raw_source_tex` | 原始导入文本 |
| `normalized_status` | raw/draft/normalized/needs_review |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |
| `last_manual_edit_at` | 最近人工编辑时间 |

设计原则：

- `Question` 只保存题目本身。
- 不保存试卷题号、教材页码、专题分组这些关系属性。
- `canonical_tex` 可以作为缓存，但不能成为唯一真相；真正字段仍是拆分字段。

### 3.2 `QuestionAnalysis`：教研分析表

用于保存朋友系统截图中那些更细的教研字段。

建议字段：

| 字段 | 说明 |
| --- | --- |
| `question_id` | 题目 ID |
| `target_tex` | 考查目的 |
| `production_tex` | 命题过程 |
| `evaluation_tex` | 试题评析 |
| `marking_data_tex` | 阅卷数据 |
| `warning_tex` | 易错警示 |
| `reference_text` | 参考文献 |
| `extra_json` | 临时扩展字段 |

这样做的好处：

- 核心题目加载更轻；
- 教研字段可按需加载；
- 未来新增字段不必频繁改 `Question` 主表。

### 3.3 `QuestionType`：题型表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `question_type_id` | 题型 ID |
| `name` | 题型名称，如单选题、填空题、解答题 |
| `code` | 题型编码 |
| `description` | 说明 |

### 3.4 `Paper`：试卷表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `paper_id` | 试卷 ID |
| `year` | 年份 |
| `paper_series` | 卷别，例如全国卷、新高考卷、地方卷 |
| `track` | 文科/理科/新高考/综合 |
| `paper_name` | 标准试卷名称 |
| `source_name` | 原始文件或原始录入中的试卷名 |
| `description` | 备注 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

### 3.5 `PaperQuestion`：试卷题目关系表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `paper_question_id` | 关系 ID |
| `paper_id` | 试卷 ID |
| `question_id` | 题目 ID |
| `question_number` | 题号 |
| `sub_number` | 小题 |
| `display_order` | 试卷内排序 |
| `origin_tex` | 原始出处 TeX |
| `location_tex` | 位置 TeX |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

设计原则：

- 同一题在多张试卷出现，建立多条 `PaperQuestion`。
- 同一题在文科、理科卷中都出现，也建立多条关系。
- 题号和小题号是“出现关系”的属性，不是题目本体属性。

### 3.6 `Book`：教材表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `book_id` | 教材 ID |
| `title` | 教材名称 |
| `publisher` | 出版社 |
| `edition` | 版本 |
| `grade` | 年级/册别 |
| `volume` | 必修/选择性必修/上册/下册 |
| `curriculum_version` | 课标版本 |
| `description` | 说明 |

### 3.7 `BookSection`：教材章节表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `section_id` | 章节 ID |
| `book_id` | 教材 ID |
| `parent_section_id` | 父章节 ID |
| `title` | 章节/小节标题 |
| `section_level` | 层级 |
| `page_start` | 起始页 |
| `page_end` | 结束页 |
| `sort_order` | 排序 |

### 3.8 `BookExerciseQuestion`：教材习题关系表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `book_exercise_question_id` | 关系 ID |
| `book_id` | 教材 ID |
| `section_id` | 章节 ID |
| `question_id` | 题目 ID |
| `page_number` | 页码 |
| `column_name` | 栏目，如例题、练习、习题、思考、探究 |
| `exercise_number` | 题号 |
| `sub_number` | 小题 |
| `display_order` | 教材内排序 |
| `source_note` | 来源备注 |

### 3.9 `TopicModule` / `Topic` / `TopicQuestion`：专题系统

`TopicModule`：

| 字段 | 说明 |
| --- | --- |
| `module_id` | 大专题 ID |
| `name` | 大专题名称 |
| `description` | 说明 |
| `sort_order` | 排序 |

`Topic`：

| 字段 | 说明 |
| --- | --- |
| `topic_id` | 小专题 ID |
| `module_id` | 所属大专题 |
| `name` | 专题名称 |
| `file_name` | 导出文件名 |
| `description` | 简介 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

`TopicQuestion`：

| 字段 | 说明 |
| --- | --- |
| `topic_question_id` | 关系 ID |
| `topic_id` | 专题 ID |
| `question_id` | 题目 ID |
| `group_name` | 分组名称 |
| `sort_order` | 专题内排序 |
| `topic_note` | 该题在该专题下的备注 |

### 3.10 `QuestionAsset`：题目资源表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `asset_id` | 资源 ID |
| `question_id` | 题目 ID |
| `role` | problem/answer/solution/source/thumbnail |
| `file_path` | 本地相对路径 |
| `original_file_name` | 原始文件名 |
| `mime_type` | 文件类型 |
| `width` | 宽度 |
| `height` | 高度 |
| `file_hash` | 文件哈希 |
| `caption` | 图片说明 |
| `sort_order` | 同一题同类图片排序 |
| `created_at` | 创建时间 |

推荐资源目录：

```text
assets/
  questions/
    Q000001/
      problem-1.png
      solution-1.png
      answer-1.png
      source-scan.pdf
```

### 3.11 `QuestionRevision`：修订记录表

建议字段：

| 字段 | 说明 |
| --- | --- |
| `revision_id` | 修订 ID |
| `question_id` | 题目 ID |
| `change_source` | manual/ai/ocr/batch_import/batch_normalize/migration |
| `changed_fields_json` | 修改字段 |
| `before_json` | 修改前快照 |
| `after_json` | 修改后快照 |
| `operator` | 操作者 |
| `note` | 修改说明 |
| `created_at` | 修订时间 |

### 3.12 `ImportBatch` 与 `ImportReportItem`

`ImportBatch`：

| 字段 | 说明 |
| --- | --- |
| `batch_id` | 批次 ID |
| `import_type` | tex/pdf/image/ocr/ai/manual |
| `source_path` | 源路径 |
| `mode` | dry_run/commit |
| `started_at` | 开始时间 |
| `finished_at` | 结束时间 |
| `summary` | 摘要 |

`ImportReportItem`：

| 字段 | 说明 |
| --- | --- |
| `item_id` | 报告项 ID |
| `batch_id` | 批次 ID |
| `source_file` | 源文件 |
| `question_id` | 题目 ID，可空 |
| `status` | inserted/updated/skipped/error/needs_review |
| `reason` | 原因 |
| `detail` | 详细信息 |
| `created_at` | 记录时间 |

---

## 4. TeX 兼容与导出设计

### 4.1 数据库存储与 TeX 表示分离

不要让数据库完全等同于某个 TeX 模板。推荐分三层：

1. **结构化字段层**
   - `stem_tex`
   - `choices_json`
   - `answer_tex`
   - `solution_tex`
   - `target_tex`
   - `production_tex`
   - `evaluation_tex`
   - `marking_data_tex`
   - `warning_tex`

2. **标准内部 TeX 层**
   - 从结构化字段生成一个统一内部 TeX。
   - 用于前端源码展示和快速复制。

3. **导出适配层**
   - 导出为当前 `chapters` 文件格式；
   - 导出为讲义 `problem` 命令；
   - 导出为试卷；
   - 导出为专题讲义；
   - 导出为教材习题集。

### 4.2 前端源码与渲染预览

未来每道题展示应保留：

- 左侧或中间：TeX 源码；
- 右侧：渲染结果；
- 顶部：题目元信息；
- 底部：保存、复制、导出、AI 辅助、图片管理。

关键状态：

- 默认只读，可复制源码；
- 点击“开始修改”后才可编辑；
- 修改中显示“保存修改”；
- 保存后生成修订记录；
- AI/OCR 生成内容先进入草稿，不直接覆盖正式内容。

### 4.3 `problem` 命令扩展原则

如果未来讲义中的 `problem` 命令需要新增字段，建议只做“导出器扩展”，不要把数据库 schema 绑定死。

例如未来可能需要：

- 来源；
- 难度；
- 标签；
- 页码；
- 教材栏目；
- 图片引用；
- 解答版本；
- 教研分析字段。

这些字段在数据库中都已结构化保存，导出时选择性写入 `problem` 参数或环境。

---

## 5. 图片系统实施方案

### 5.1 第一版能力

第一版图片功能建议支持：

- 上传题干图；
- 上传解析图；
- 上传答案图；
- 上传原始扫描图；
- 在题干/答案/解析 TeX 中插入图片占位符；
- 前端渲染时显示真实图片；
- 导出 LaTeX 时转换为 `\includegraphics`。

### 5.2 图片占位符建议

内部可以设计一种稳定占位符，例如：

```tex
\questionasset{problem-1}
```

或：

```tex
\includegraphics{assets/questions/Q000001/problem-1.png}
```

更推荐前者。原因：

- 数据库可以根据 `asset_id` 或别名解析路径；
- 题目移动、导出到不同目录时不需要改正文；
- 导出器能统一处理图片复制和相对路径。

### 5.3 图片导出策略

导出试卷或讲义时：

1. 找到题目用到的所有图片资源。
2. 复制到导出目录，例如 `figures/`。
3. 把内部占位符转换成相对路径。
4. 生成可编译的 LaTeX。

---

## 6. 前端功能重构规划

### 6.1 录入新题

目标页面结构：

```text
顶部：格式化 / 检查 / 编译 / 复制 / 清空 / 确认入库 / 仅导出 tex

左侧：题目定位与元信息
  年份
  题源
  题号
  小题
  题型
  难度
  标签
  备注
  参考文献

中间：结构化内容编辑
  题干
  答案
  解析
  考查目的
  命题过程
  试题评析
  阅卷数据
  易错警示

右侧：渲染预览 / 校验结果 / 图片列表
```

第一版可以先不完全照搬朋友页面，但必须具备：

- 结构字段明确；
- TeX 源码可见；
- 右侧渲染预览；
- 保存前校验；
- AI/OCR 进入草稿；
- 确认后写数据库。

### 6.2 全局浏览与编辑

未来全局浏览不应直接扫描所有 `.tex` 文件，而应查询数据库索引。

优化目标：

- 默认分页；
- 支持每页 `5 / 10 / 15 / 20`；
- 难度星级、标签、备注从数据库轻量加载；
- 题目详情、解答、图片按需加载；
- 左侧搜索/筛选吸附滚动；
- 右侧题目卡支持源码和渲染双视图。

### 6.3 组卷服务

组卷篮应作为 UI 状态和持久草稿之间的中间层：

- UI 中右侧悬浮试题篮；
- 支持拖拽排序；
- 支持一次点击移除；
- 支持大小调整；
- 不触发整页刷新；
- 可保存为组卷草稿；
- 可导出为试卷或讲义。

未来可以加表：

| 表 | 说明 |
| --- | --- |
| `PaperDraft` | 组卷草稿 |
| `PaperDraftItem` | 草稿中的题目、排序、分值、备注 |

### 6.4 试卷库

试卷库第一版功能：

- 新建试卷；
- 编辑试卷元信息；
- 关联已有题目；
- 按年份、卷别、文理/新高考、试卷名浏览；
- 从题目反查所属试卷；
- 一键导出试卷 TeX。

### 6.5 教材库

教材库第一版功能：

- 新建教材；
- 建立章节树；
- 录入页码和栏目；
- 关联题目；
- 从题目反查所属教材位置；
- 按教材章节导出习题。

### 6.6 专题库

专题库第一版功能：

- 新建大专题；
- 新建小专题；
- 新增分组；
- 通过搜索或题目 ID 添加题目；
- 支持专题内排序；
- 支持专题内备注；
- 导出专题讲义。

---

## 7. 当前项目结构审视

当前根目录主要内容包括：

```text
question_bank_app.py
services/
utils/
chapters/
fig/
cover/
Test Paper Group/
tiku/
.backups/
.sync-work-20260819/
.venv/
README.md
requirements.txt
main.tex
MathCyclus_book.cls
题库重构参考沉淀.md
```

### 7.1 当前主要问题

#### 7.1.1 `question_bank_app.py` 过大

当前主程序约 590KB。它已经承担了太多职责：

- 页面布局；
- 状态管理；
- 题目读取；
- 题目保存；
- 搜索；
- 组卷；
- TeX 预览；
- CSS 注入；
- UI 交互；
- 业务逻辑。

后续数据库重构时，不应继续在这个文件里堆功能。建议逐步拆为：

```text
app/
  pages/
    global_browser.py
    question_entry.py
    paper_builder.py
    paper_library.py
    book_library.py
    topic_library.py
  components/
    question_card.py
    tex_editor.py
    render_preview.py
    sticky_sidebar.py
    asset_panel.py
  state/
    basket_state.py
    search_state.py
```

如果暂时不迁移到 `app/`，至少新功能应先放入 `services/` 和 `utils/`，页面只调用服务。

#### 7.1.2 `services/` 已经是正确方向

当前已有：

```text
services/
  ai_service.py
  file_service.py
  operation_log.py
  question_service.py
  semantic_search_service.py
```

后续可以扩展：

```text
services/
  database_service.py
  paper_service.py
  book_service.py
  topic_service.py
  asset_service.py
  import_service.py
  export_service.py
  revision_service.py
```

#### 7.1.3 `utils/` 内容需要分层

当前 `utils/` 同时包含：

- LaTeX 工具；
- CSV 工具；
- 统计工具；
- 图表；
- 本地索引；
- 自定义 Streamlit 组件；
- 项目文档。

后续建议把“纯工具”和“前端组件”分开：

```text
utils/
  latex_ops.py
  tikz_ops.py
  file_ops.py

components/
  star_rating/
  sortable_list/
```

或者保留当前目录，但文档中明确职责，避免新功能继续混放。

#### 7.1.4 本地运行目录应继续忽略

当前这些目录不应进入 Git：

- `.venv/`
- `.backups/`
- `.sync-work-*`
- `.codex/`
- `.agents/`
- `.tikz_cache/`
- `.edge-test-profile/`
- `cloze_exports/`
- 临时日志、缓存数据库、语义索引。

当前 `.gitignore` 已包含不少规则，但文件内容出现终端乱码显示。后续可以单独整理 `.gitignore` 为 UTF-8 可读中文，避免维护困难。

#### 7.1.5 `tiku/` 是独立实验区

当前 `tiku/` 约 13MB，`.gitignore` 已标注为“独立前端实验工作区”。后续要明确：

- 如果它不是当前 Streamlit 应用的一部分，应继续忽略。
- 如果其中有可复用 UI，需要迁移到正式 `app/components/` 或 `frontend/` 后再纳入版本管理。

#### 7.1.6 `Test Paper Group/` 需要重新定位

该目录包含导出的试卷/讲义模板和历史导出。当前 Git 中似乎已有部分文件被跟踪。后续建议：

- 模板文件可以保留跟踪；
- 用户生成的导出文件不应跟踪；
- 建议拆为：

```text
templates/
  paper/
  handout/

exports/
  papers/
  handouts/
```

并让 `exports/` 默认进入 `.gitignore`。

### 7.2 建议删除或清理的内容

注意：这里是规划，不是立即删除。

| 路径 | 建议 | 理由 |
| --- | --- | --- |
| `old_app.py` | 可删除或保留在本地备份，不进 Git | 空文件或历史残留 |
| `question_bank_app000.py` | 不建议继续保留在根目录 | 历史备份应进 `.backups/`，不应混在源码根目录 |
| `ocr的备份.txt` | 移入 `docs/archive/` 或删除 | 根目录杂项备份 |
| `更新信息.txt` | 迁移为 `CHANGELOG.md` 或 `docs/logs/` | 当前命名不利于长期维护 |
| `log.csv` | 不进 Git，作为运行日志 | 已在 `.gitignore` |
| `2009-2025高考数学全国卷.pdf` | 不进 Git，放入本地资料目录 | 大文件且可能涉及版权 |
| `.backups/` | 只本地保留 | 自动备份目录，不进 Git |
| `.sync-work-*` | 删除或本地保留，不进 Git | 临时同步目录 |
| `.venv/` | 不进 Git | 虚拟环境 |
| `tiku/` | 确认用途后决定 | 独立实验区不应混入主应用 |

---

## 8. 推荐文件管理规范

### 8.1 未来目录结构建议

建议逐步演进为：

```text
MathCyclus/
  question_bank_app.py
  README.md
  requirements.txt
  .gitignore

  app/
    pages/
    components/
    state/
    styles/

  services/
    question_service.py
    paper_service.py
    book_service.py
    topic_service.py
    asset_service.py
    import_service.py
    export_service.py
    revision_service.py

  db/
    schema.sql
    migrations/
    seed/

  data/
    mathcyclus.sqlite3
    indexes/

  assets/
    questions/
    app/

  chapters/
    ...

  exports/
    papers/
    topics/
    books/

  templates/
    tex/
    paper/
    handout/

  docs/
    planning/
    logs/
    archive/

  scripts/
    migrate_tex_to_db.py
    export_db_to_tex.py
    scan_tex_library.py
    backup_database.py

  tests/
```

### 8.2 Git 管理原则

应进入 Git：

- 源码；
- 数据库 schema；
- 迁移脚本；
- 模板；
- 文档；
- 小型示例数据；
- 必要 UI 资源。

不应进入 Git：

- `.env`；
- API key；
- 虚拟环境；
- 自动备份；
- 编译缓存；
- 大型 PDF；
- 用户生成的导出试卷；
- 运行日志；
- 本地 SQLite 正式库；
- 语义索引缓存；
- OCR 中间文件；
- 大量题库原始文件，如果涉及版权或体积风险。

### 8.3 数据文件命名原则

建议统一：

- 题目 ID：`Q000001`
- 图片：`problem-1.png`、`solution-1.png`
- 导入批次：`import_20260901_001`
- 备份：`mathcyclus_20260901_001.sqlite3`
- 导出：`2026-全国新高考I卷.tex`
- 日志：`2026-09-01-数据库结构设计.md`

---

## 9. 分阶段实施路线

### 阶段 0：冻结当前稳定版本

目标：确保现有功能有可回退点。

任务：

- 确认当前 Git 状态干净或明确哪些改动属于当前版本。
- 推送当前稳定版本到 GitHub。
- 确认 `.gitignore` 不会上传 `.env`、备份、大 PDF、虚拟环境。
- 记录当前版本说明。

产出：

- 一个可回退的 GitHub commit。
- 一个简短版本日志。

### 阶段 1：整理项目文件管理

目标：避免重构过程中根目录继续混乱。

任务：

- 建立 `docs/planning/`、`docs/logs/`、`docs/archive/`。
- 把长期规划文档移入 `docs/planning/`，或保留根目录同时在 README 中索引。
- 整理 `.gitignore` 为可读 UTF-8。
- 明确 `Test Paper Group/` 中哪些是模板，哪些是导出产物。
- 确认 `tiku/` 是否保留为实验区。

产出：

- 清晰目录规范。
- 可读 `.gitignore`。
- 不误上传本地文件。

### 阶段 2：设计数据库 schema

目标：先把表结构定下来，不碰现有 UI。

任务：

- 新建 `db/schema.sql`。
- 定义 `Question`、`QuestionAnalysis`、`Paper`、`PaperQuestion`。
- 定义 `Book`、`BookSection`、`BookExerciseQuestion`。
- 定义 `TopicModule`、`Topic`、`TopicQuestion`。
- 定义 `QuestionAsset`、`QuestionRevision`、`ImportBatch`、`ImportReportItem`。
- 写字段说明文档。

产出：

- `db/schema.sql`
- `docs/planning/数据库字段说明.md`

### 阶段 3：建立数据库服务层

目标：让后续页面不直接操作 SQLite。

任务：

- 新建 `services/database_service.py`。
- 新建 `services/question_db_service.py`。
- 新建 `services/paper_service.py`。
- 新建 `services/book_service.py`。
- 新建 `services/topic_service.py`。
- 新建 `services/asset_service.py`。
- 新建 `services/revision_service.py`。
- 提供最小 CRUD。

产出：

- 可创建空库；
- 可插入/读取题目；
- 可记录修订。

### 阶段 4：扫描现有 `.tex` 题库

目标：先生成报告，不直接迁移。

任务：

- 扫描 `chapters/`。
- 解析每个 `.tex` 文件的：
  - 文件路径；
  - 当前 ID；
  - 难度星级；
  - 标签；
  - 备注；
  - 组卷引用次数；
  - problem；
  - answer；
  - solutions；
  - 图片引用。
- 检查：
  - 缺失 ID；
  - 重复 ID；
  - 无法解析；
  - 图片路径异常；
  - 标签格式异常；
  - 难度字段异常。

产出：

- `reports/tex_scan_日期.md`
- `reports/tex_scan_日期.csv`

### 阶段 5：生成稳定 `question_id`

目标：建立旧文件路径到新 ID 的映射。

任务：

- 为每道题生成 `Q000001` 形式 ID。
- 建立 `legacy_file_path` 映射。
- 如果原文件已有可靠 ID，可以记录为 `legacy_id`。
- 不改原 `.tex` 文件，先只写数据库或映射表。

产出：

- `Question` 初始记录；
- `legacy_question_map` 或等价映射表；
- 映射报告。

### 阶段 6：迁移题目核心内容

目标：把题目核心字段入库。

任务：

- 迁移题干、答案、解析；
- 迁移难度、标签、备注；
- 迁移组卷引用次数；
- 迁移图片引用为初步资源记录；
- 保留原始 TeX；
- 生成 `QuestionRevision`，来源为 `migration`。

产出：

- 初版可查询数据库题库；
- 可从数据库还原题目源码。

### 阶段 7：建立试卷关系

目标：把年份、卷别、文理/新高考、试卷名、题号、小题从题目本体中拆出来。

任务：

- 从文件名或题干来源解析试卷信息。
- 建立 `Paper`。
- 建立 `PaperQuestion`。
- 对无法确定的记录生成待确认报告。

产出：

- 可按试卷浏览题目；
- 可从题目反查所属试卷。

### 阶段 8：建立 TeX 导出器

目标：证明数据库可以反向生成当前工作流需要的 `.tex`。

任务：

- 实现 `ExportService`。
- 支持单题导出；
- 支持按试卷导出；
- 支持按知识板块导出；
- 支持生成讲义 `problem` 命令；
- 支持图片路径复制和重写。

产出：

- 数据库 → `.tex` 的闭环能力。
- 旧文件式工作流不被破坏。

### 阶段 9：重构全局浏览

目标：全局浏览从数据库读取，而不是每次扫描文件。

任务：

- 题目列表读取数据库分页结果。
- 详情按需加载。
- 难度、标签、备注从数据库快速显示。
- 保留源码 + 渲染双栏。
- 支持跳转原始 `.tex` 文件。

产出：

- 更快的全局浏览；
- 数据库主存储开始进入日常使用。

### 阶段 10：重构录入新题

目标：实现类似朋友截图但适合 MathCyclus 的结构化录入页。

任务：

- 左侧元信息；
- 中间字段编辑；
- 右侧渲染预览；
- 顶部工具栏；
- 图片附件面板；
- 保存前校验；
- 保存后写数据库和修订记录；
- 支持仅导出 TeX。

产出：

- 新题直接进入数据库；
- 仍可导出 `.tex`。

### 阶段 11：实现 AI/OCR 草稿录入

目标：让 AI/OCR 进入可控工作流。

任务：

- 新建草稿表或草稿状态；
- AI 解析原始文本；
- 自动填字段；
- 不确定项标记；
- 预览与校验；
- 用户确认写入正式库；
- 生成导入报告。

产出：

- 类似朋友系统的 AI 辅助录入；
- 不直接污染正式题库。

### 阶段 12：实现图片附件系统

目标：完整支持非 TikZ 图。

任务：

- 上传图片；
- 存入 `assets/questions/<question_id>/`；
- 记录 `QuestionAsset`；
- 在 TeX 中插入占位符；
- 前端预览；
- 导出时复制图片并改写路径。

产出：

- 题干、答案、解析都可稳定使用图片。

### 阶段 13：建立教材库

目标：支持书籍/章节/页码/栏目/题号。

任务：

- 新建教材；
- 建立章节树；
- 录入栏目；
- 关联题目；
- 反查题目来源；
- 按章节导出。

产出：

- 教材题库开始可用。

### 阶段 14：建立专题库

目标：支持大专题、小专题、分组、排序、备注。

任务：

- 新建专题模块；
- 新建专题；
- 添加题目；
- 拖拽排序；
- 分组管理；
- 专题导出。

产出：

- 专题库开始可用。

### 阶段 15：组卷服务数据库化

目标：让组卷篮和组卷草稿更稳定。

任务：

- 试题篮继续作为前端悬浮 UI；
- 组卷草稿保存到数据库；
- 支持恢复草稿；
- 支持题目排序、分值、备注；
- 支持导出试卷/讲义。

产出：

- 组卷服务不依赖临时 session；
- 刷新页面后草稿可恢复。

---

## 10. 第一批最小可执行任务

如果下一步开始落地，建议不要直接做全部。第一批只做这些：

1. 整理 `.gitignore` 可读性。
2. 建立 `docs/`、`db/`、`reports/`、`scripts/` 基础目录。
3. 写 `db/schema.sql` 第一版。
4. 写扫描脚本 `scripts/scan_tex_library.py`，只读不写。
5. 输出现有题库扫描报告。
6. 设计 `question_id` 规则。
7. 写迁移脚本 dry-run 版本。

第一批完成后再决定是否真正迁移。

---

## 11. 判断标准

每个阶段完成时，需要满足：

- 当前应用还能运行；
- 现有 `.tex` 不被破坏；
- 数据库变更有 schema 文件；
- 迁移有报告；
- 写入有备份；
- AI/OCR 不直接覆盖正式数据；
- 图片资源路径可追踪；
- Git 不包含本地缓存和隐私文件。

---

## 12. 总结

接下来的重构重点不是“把页面做得更像朋友的系统”，而是建立一个能支撑未来十倍复杂度的数据底座。

最终目标是：

- 数据库是主真相；
- TeX 是可导出的兼容格式；
- 图片是稳定挂接资源；
- 试卷、教材、专题都是独立业务对象；
- 题目通过 `question_id` 与所有对象建立关系；
- 前端继续提供源码编辑与右侧渲染；
- AI/OCR 只进入草稿和报告，不直接污染正式库；
- 项目文件结构清楚，Git 仓库轻、稳、可维护。

---

## 13. 2026-09-02 当前落地进度快照

截至本轮，已完成的是“安全重构底座”，不是现有 Streamlit 前端替换。

已完成闭环：

1. 旧 `.tex` 题库扫描：
   - 已能扫描 `chapters/` 下正式题目；
   - 已能排除汇总文件和相关图辅助 TeX；
   - 已能报告缺图、解析异常和基础元数据。

2. TeX 到 SQLite 迁移 dry-run：
   - 已生成 893 道题的预览数据库；
   - 已建立 `question`、`question_analysis`、`legacy_question_map`；
   - 已为旧文件分配 `Q000001` 形式稳定 ID。

3. 试卷关系：
   - 已建立 `paper` 和 `paper_question`；
   - 已生成试卷名称映射表；
   - 已支持试卷名称映射 dry-run；
   - 已支持从试卷查题、从题反查试卷。

4. 知识板块与同题关系：
   - 已建立 `knowledge_area`、`question_knowledge_area`、`question_equivalence`；
   - 当前识别知识板块 23 个；
   - 当前题目-知识板块关系 893 条；
   - 当前疑似同题关系 3 条。

5. 重复题/同题人工审查：
   - 已生成 `db/seed/equivalence_review_decisions_20260902_initial.csv`；
   - 当前待审行 10 条；
   - 已支持 `pending`、`keep_all`、`mark_equivalent`、`merge_to_canonical`、`ignore`；
   - 已支持在数据库副本上应用决策并输出报告；
   - 已有审查辅助脚本 `scripts/review_equivalence_decisions.py`。

6. 试卷映射人工审查：
   - 已有 `db/seed/paper_name_mapping.csv`；
   - 当前仍有 301 行 `pending`；
   - 已有审查辅助脚本 `scripts/review_paper_mapping.py`；
   - 当前不应由脚本静默自动批准全部映射。

7. 图片资产：
   - 已建立 `question_asset`；
   - 已支持扫描旧 `相关图` 目录并登记图片资产；
   - 当前发现并匹配 7 张旧题库图片；
   - 当前仍有缺失 `includegraphics` 引用，需要人工找回原图或改用 `questionasset` 占位符。

8. AI/OCR 草稿录入：
   - 已建立 `question_import_draft` 和 `question_import_draft_asset`；
   - 已建立 `import_batch` 和 `import_report_item`；
   - 已支持 JSON 草稿 dry-run 导入；
   - 已支持草稿提交预演，预演新增 `Q000894` 和 1 条 `question_revision`；
   - 当前不直接写入正式题库。

9. 教材库：
   - 已建立 `book`、`book_section`、`book_exercise_question`；
   - 已支持教材 JSON dry-run 导入；
   - 示例已能记录页码、栏目、题目关系。

10. 专题库：
    - 已建立 `topic_module`、`topic`、`topic_question`；
    - 已支持专题 JSON dry-run 导入；
    - 专题结构保持轻量，方便未来继续扩展。

11. TeX 导出：
    - 已支持从 SQLite 预览库导出 `.tex`；
    - 已支持 `\begin{problem}` 兼容输出；
    - 已支持图片资源占位符解析与导出提示；
    - 原 `.tex` 仍作为迁移来源、备份和未来导出目标。

12. 正式库提升与安全：
    - 已有 `scripts/backup_database.py`；
    - 已有 `scripts/precommit_database_audit.py`；
    - 已有 `scripts/promote_preview_to_database.py`；
    - 正式写入必须显式 `--apply --confirm PROMOTE_SQLITE_PREVIEW`；
    - 当前 `data/mathcyclus.sqlite3` 仍不存在；
    - 当前提升流程仍处于 dry-run 阶段。

13. 查询、流水线和项目卫生：
    - 已有只读浏览脚本 `scripts/browse_question_db.py`；
    - 已有进度汇总脚本 `scripts/rebuild_status.py`；
    - 已有一键预览重建流水线 `scripts/rebuild_preview_pipeline.py`；
    - 已有提交前项目卫生检查 `scripts/check_project_hygiene.py`；
    - 已有数据 warning 人工审查包脚本 `scripts/generate_data_warning_review_pack.py`；
    - 已生成重复题位、缺失图片、导入草稿三类人工审查 CSV；
    - 卫生检查确认本轮没有未忽略数据库、生成产物或正式库误创建风险。

当前综合预览库：

- 路径：`data/mathcyclus_preview_combined_20260902_initial.sqlite3`
- `question`：893
- `paper`：301
- `paper_question`：893
- `knowledge_area`：23
- `question_knowledge_area`：893
- `question_equivalence`：3
- `question_asset`：7
- `question_import_draft`：2
- `book`：1
- `topic`：2

当前提交预演库：

- 路径：`data/mathcyclus_preview_combined_20260902_commit_preview.sqlite3`
- 新增示例题：`Q000894`
- 新增修订记录：1 条
- 未删除已有稳定题目 ID

当前已知 warning：

1. 试卷映射表仍有 301 行 `pending`，需要人工审查。
2. 同题决策表仍有 10 行 `pending`，需要人工审查。
3. 综合预览库存在同一试卷同一题号重复位置，需要确认是否是小题、重复录入或真实同题。
4. 存在缺失图片引用，需要找回原图或改用 `questionasset` 占位符。
5. 综合预览库包含 `needs_review` 草稿，正式入库前不能自动提交。

当前明确未做：

- 未把数据库接入 `question_bank_app.py`；
- 未修改现有前端 UI；
- 未修改原始 `.tex` 题库；
- 未正式合并重复题；
- 未正式提交 AI/OCR 草稿；
- 未创建正式 SQLite 库；
- 未执行 Git push。

下一步最稳妥顺序：

1. 用 `scripts/review_paper_mapping.py` 逐步审查并更新 `db/seed/paper_name_mapping.csv`。
2. 用 `scripts/review_equivalence_decisions.py` 逐步审查并更新同题决策 CSV。
3. 用 `scripts/generate_data_warning_review_pack.py` 生成的三份 CSV 处理重复题位、缺失图片、`needs_review` 草稿。
4. 重建综合预览库并运行审计、卫生检查和提升 dry-run。
5. 人工确认后再正式生成 `data/mathcyclus.sqlite3`。
6. 最后再把全局浏览、录入新题、组卷服务逐步切换到 SQLite 只读/草稿模式。

进度估算：

- 数据底座和 dry-run 工具链：约 75%～80%；
- 人工审查与数据清洗：约 20%～30%；
- 正式库创建与前端迁移：尚未开始；
- 整体数据库重构计划：约 55%～60%。

---

## 14. 2026-09-02 正式库与只读服务进展

本节覆盖第 13 节中的旧状态：缺图阻塞已处理，正式 SQLite 已创建。

### 14.1 缺失图片引用处理

`Q000696` 原有 3 个缺失 `includegraphics` 引用：

1. `images/2021 新高考I 卷 12答案1.png`
2. `images/2021 新高考I 卷 12答案2.png`
3. `images/2021 新高考I 卷 12答案3.png`

处理决策：

- 当前工作区找不到原图片；
- 先删除数据库副本中的引用，避免阻塞正式库；
- 修正进入 `db/seed/question_tex_corrections_20260902_final_review.csv`；
- 修正字段覆盖 `solution_tex`、`canonical_tex`、`raw_source_tex`；
- 不修改 `chapters/` 下原始 `.tex` 题库。

字段级验证：

```text
Q000696.stem_tex includegraphics=0
Q000696.answer_tex includegraphics=0
Q000696.solution_tex includegraphics=0
Q000696.canonical_tex includegraphics=0
Q000696.raw_source_tex includegraphics=0
```

### 14.2 正式 SQLite 状态

正式库路径：

- `data/mathcyclus.sqlite3`

当前正式库核心规模：

- `question`：893
- `paper`：302
- `paper_question`：888
- `knowledge_area`：23
- `question_knowledge_area`：893
- `question_equivalence`：8
- `question_asset`：7
- `book`：1
- `book_section`：3
- `book_exercise_question`：2
- `topic_module`：1
- `topic`：2
- `topic_question`：3

正式库提升结果：

```text
can_apply=True
written=True
blockers=0
warnings=0
```

正式库文件仍应被 `.gitignore` 忽略，不纳入 Git。

### 14.3 只读查询服务准备

本轮完成正式库接入前的服务层准备：

- `services/database_service.py` 新增 `connect_existing_database`；
- `services/database_service.py` 新增 `readonly_database_connection`；
- 浏览查询不再通过普通可写连接打开数据库；
- 如果目标数据库不存在，只读查询会报错，不会静默创建空库。

已切换为只读连接的服务：

- `services/question_db_service.py`
- `services/paper_service.py`
- `services/knowledge_service.py`
- `services/asset_service.py`
- `services/book_service.py`
- `services/topic_service.py`
- `services/equivalence_service.py`
- `services/revision_service.py`
- `services/import_service.py` 中的查询函数

### 14.4 面向前端的查询能力

`services/question_db_service.py` 增强：

- `QuestionListFilters` 支持 `keyword`、`year`、`chapter`、`source`、`question_number`、`question_type_id`、`difficulty`、`limit`、`offset`；
- `list_questions_page` 返回 `total`、`limit`、`offset`、`page`、`page_count`、`items`；
- `list_question_filter_options` 返回年份、章节、来源、难度、题型和分页选项；
- `get_question_bundle` 一次性返回题目本体、图片资产、试卷来源、教材来源、专题来源。

`scripts/browse_question_db.py` 增强：

- 默认数据库改为 `data/mathcyclus.sqlite3`；
- `questions` 支持分页偏移和更多筛选参数；
- 新增 `question-filters`；
- 新增 `question-bundle`。

示例命令：

```text
python scripts/browse_question_db.py summary
python scripts/browse_question_db.py question-filters
python scripts/browse_question_db.py questions --year 2025 --limit 20 --offset 0
python scripts/browse_question_db.py question-bundle Q000001
```

### 14.5 验证入口

新增服务层烟测：

- `scripts/smoke_database_services.py`

用途：

- 在不启动 Streamlit 的情况下验证正式库和服务层是否可用；
- 每次准备接入前端前先运行；
- 用于发现数据库缺失、查询函数异常、筛选项为空、bundle 缺失等问题。

验证命令：

```text
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
```

当前结果：

```text
status=ok
question_count=893
paper_count=302
```

### 14.6 下一阶段顺序

接下来建议按以下顺序继续：

1. 继续完善数据库服务层；
2. 全局浏览页先接入 SQLite 只读适配层，但不替换旧 TeX 文件读取；
3. 做“SQLite 只读预览开关”，允许前端在旧模式和数据库模式之间切换；
4. 全局浏览稳定后，再迁移组卷服务的题目读取；
5. 录入新题先写入 `question_import_draft`，人工确认后再进入正式 `question`；
6. 图片插入功能放在数据库写入链路稳定之后，再落地到 `question_asset` 与前端预览；
7. 每完成一阶段都运行 `precommit_database_audit`、`smoke_database_services` 和 `check_project_hygiene`。

### 14.7 全局浏览 SQLite 只读预览入口

已完成第一步前端接入：

- `question_bank_app.py` 在“全局浏览与编辑”页面顶部增加 `使用 SQLite 只读预览模式` 开关；
- 默认关闭，不影响旧全局浏览、编辑、AI 解答和组卷逻辑；
- 开启后进入独立 SQLite 只读页面；
- 只读取 `data/mathcyclus.sqlite3`；
- 不保存、不编辑、不修改旧 `.tex` 文件。

预览页面能力：

- 左侧筛选：年份、知识板块、试卷来源、题型、难度、关键词；
- 分页：每页 `5`、`10`、`15`、`20`；
- 右侧题目展示：TeX 源码和 LaTeX 渲染预览左右并排；
- 附加信息：试卷来源、教材来源、专题来源、图片资源可折叠查看。

### 14.8 SQLite 左侧筛选与搜索态增强

在只读预览入口的基础上，继续补齐左侧操作体验：

- 知识板块使用下拉栏，避免按钮网格挤压左侧栏；
- 年份使用下拉栏，保持左侧筛选宽度稳定；
- 关键词搜索改为显式 `开始搜索` / `再次搜索`，避免输入时频繁刷新；
- 搜索激活后显示检索模式状态与 `退出检索`；
- 查找结果区域显示总数、每页数量、当前页码；
- 查找结果区域新增页码下拉栏，可直接跳转到指定页；
- 查找结果下方新增“题目显示”下拉栏；
- `题目显示` 默认展示当前页全部题目，也可切换到当前页单道题；
- 试卷来源、题型、难度保留在“更多筛选”中；
- 每页数量移动到查找结果区域，与页码控制一起管理。

右侧展示也做了收敛：

- 每道题仍保持一层外部题目卡；
- 不再额外叠加自定义内层卡片边框；
- 当前已进一步取消 SQLite 预览题目卡的 Streamlit 外层边框，减少右侧多框线观感；
- TeX 源码和渲染预览继续左右并排；
- 来源关系与图片资源仍在折叠区中只读查看。

左侧栏后续体验：

- SQLite 预览左侧栏已加宽；
- 左侧筛选卡片已改为粘性吸附；
- 页面滚动时左侧栏跟随保留在视口内；
- 左侧栏内容过高时内部滚动，避免底部分页与题目选择不可见。
- 左栏宽度在人工反馈后已回退到较早版本；
- 相关 CSS 已从宽泛 `:has(.anchor)` 改为直接子级锚点匹配，避免误作用到整页祖先容器。

这一阶段仍然只读，不提供数据库写入。

### 14.9 SQLite 编辑服务层预备

已先完成后续数据库编辑所需的服务层底座，但尚未暴露到前端：

- `services/database_service.py` 新增 `existing_database_connection`；
- `services/revision_service.py` 新增 `insert_question_revision_from_conn`；
- `services/question_edit_service.py` 新增 `update_question_fields`；
- `services/question_edit_service.py` 新增 `question_to_edit_form` 与 `edit_form_to_question_updates`；
- `services/question_edit_service.py` 新增 `get_question_edit_state`；
- `scripts/smoke_question_edit_service.py` 新增临时副本写入 smoke；
- `scripts/browse_question_db.py` 新增 `question-edit-state` 只读调试命令；
- 编辑接口只允许更新白名单字段；
- `choices_json`、`tags_json`、`difficulty`、`official_flag` 会先规范化；
- 真实变更会在同一事务内更新 `question` 并写入 `question_revision`；
- 临时副本 smoke 已验证写入和 revision，不修改正式库。
- 表单模型已预备第一阶段前端字段：题型、题干、选项、答案、解答、难度、标签、备注、官方标记；
- 编辑状态接口已能一次性返回表单值、可见字段、题型选项和修订记录；
- 已避免完整表单提交时未修改 JSON 字段被格式化误判为变更。

下一步前端数据库编辑方案必须单独实现：

1. SQLite 预览页面默认只读；
2. 点击 `开始修改` 后进入编辑态；
3. 编辑 TeX 副本与题目信息；
4. 点击 `保存修改` 后调用 `update_question_fields` 写入 SQLite；
5. 每次写入必须记录 `question_revision`；
6. 后续由导出器输出兼容旧 `problem` 命令的 `.tex`。

验证：

```text
python -m py_compile question_bank_app.py services/database_service.py services/question_db_service.py scripts/smoke_database_services.py
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/browse_question_db.py question-filters
python scripts/browse_question_db.py questions --year 2025 --limit 5 --offset 0
python scripts/browse_question_db.py question-bundle Q000001
```

当前限制：

- 该入口仍是只读预览；
- 尚未替代旧全局浏览；
- 尚未接入数据库编辑；
- 尚未接入数据库组卷篮。

当前进度估算：

- 数据底座和正式库：约 90%；
- 服务层查询能力：约 75%；
- 服务层编辑能力：约 55%，已具备受控字段写入、revision 记录和前端表单转换，但尚未接前端；
- 前端 SQLite 接入：只读预览与左侧筛选增强已开始；
- 图片新增/编辑工作流：尚未开始；
- 整体数据库重构计划：约 72%。

---

### 14.10 SQLite ?????????

? `14.9` ?????????????????????????????????????????????? SQLite???? TeX ?????????

????

- `question_bank_app.py` ? SQLite ?????????? `????`?
- ????????????????
- ?? `????` ???????????
- ?????????????????????????????????????
- ?? `????` ?????? `update_question_fields`?
- ????? `data/mathcyclus.sqlite3`?
- ?????? `chapters/` ?? `.tex` ???
- ????????? `question_revision`?
- ??????????? `canonical_tex`??????????? TeX ???
- ?? `????` ???????????????
- ???????????????????

???????

- ???????
- ???????????????
- revision ?? UI?
- SQLite ????????????????
- ??????? SQLite ????

?????????

- ?????????? 90%?
- ????????? 80%?
- ????????? 65%?
- ?? SQLite ???? 45%?????????????????????
- ????/???????????
- ??????????? 76%?

---

### 14.11 ????????????

?????? SQLite ???????????????????

????

- SQLite ????? `???? / ????` ????????????
- `question_asset` ???????????????
- ?????????? 2MB ????????????
- ????????asset_id????????????
- `services/asset_service.py` ?? `attach_asset_to_question()`?
- attach ????????????????????
- attach ????????
- attach ?????? `assets/questions/<question_id>/` ??? `question_asset`?
- ?????????mime?hash?caption?sort_order?
- SQLite ?????????/PDF ???????
- ????????????????
- ????????? `.tex` ???

?????

- ??????????? `\questionasset{...}` ????/??/???
- ?????????????????????
- ?????????????? revision?
- ??????????????

?????????

- ?????????? 90%?
- ????????? 80%?
- ????????? 68%?
- ?????????? 45%?
- ?? SQLite ???? 52%?????????????????????????????????
- ??????????? 78%?

---

### 14.12 图片占位符、资源维护与 SQLite 导出入口

本轮目标是在不改旧 `.tex` 题库、不替换旧全局浏览/组卷逻辑的前提下，把 SQLite 单题编辑里的图片资源工作流继续向“可实际录入、可引用、可导出”推进。

已完成：

- `services/asset_service.py` 新增 `preferred_asset_alias()` 和 `asset_placeholder()`；
- 默认占位符格式为 `\questionasset{文件名不带后缀}`，例如 `\questionasset{source}`；
- 占位符仍可在导出阶段通过 `asset_id`、文件 stem、文件名、原始文件 stem、原始文件名解析；
- `attach_asset_to_question()` 返回 `placeholder` 和 `revision_id`；
- 图片/附件登记会写入 `question_asset`，并额外写入一条 `question_revision` 事件；
- 新增 `get_asset()`、`update_asset_fields()`、`delete_asset()`；
- 资源元数据目前允许编辑 `role`、`caption`、`sort_order`；
- 资源移除默认只删除 SQLite 登记，不删除真实文件；
- 如果未来确实要删除真实文件，服务层只允许删除 `assets/questions/` 内的单个文件；
- SQLite 单题编辑态下可以查看已登记资源；
- 已登记资源支持复制占位符、插入到题干/答案/解析 TeX 字段；
- 已登记资源支持修改位置、说明、排序和移除登记；
- 只读资源卡片中会显示对应的 `\questionasset{...}` 占位符；
- SQLite 只读卡片新增 `下载 TeX`；
- SQLite 编辑预览区新增 `下载草稿 TeX`；
- `scripts/export_db_to_tex.py` 默认数据库改为正式库 `data/mathcyclus.sqlite3`；
- `scripts/precommit_database_audit.py` 和 `scripts/audit_question_assets.py` 的 `questionasset` 解析规则已扩展到多种别名，避免误报。

验证：

```text
python -m py_compile question_bank_app.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_asset_placeholder_ui
python scripts/export_db_to_tex.py --limit 1 --stamp 20260902_asset_placeholder_ui --resolve-questionassets
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- Python 编译通过；
- asset smoke 通过，且只写临时库；
- question edit smoke 通过，且只写临时库；
- database service smoke 通过；
- precommit database audit：`blockers=0`，`warnings=0`；
- SQLite 导出脚本成功导出 1 道题；
- UI layout detector 返回 `[]`。

当前进度估算：

- 数据底座和正式库：约 90%；
- 服务层查询能力：约 80%；
- 服务层编辑能力：约 72%；
- 图片资源服务：约 58%；
- 前端 SQLite 接入：约 60%；
- SQLite 导出能力：约 35%；
- 整体数据库重构计划：约 82%。

下一步建议：

1. 做图片资源在题目预览中的真实前端渲染，而不是只在资源折叠区显示缩略图；
2. 做 SQLite 新题录入入口，先进入草稿表，再人工确认入正式题库；
3. 做 AI/OCR 导入草稿的前端审核页；
4. 做按试卷/教材/专题维度的 SQLite 导出；
5. 再考虑让新 SQLite 结构逐步替代旧文件浏览。

---

### 14.13 SQLite 预览中的非 TikZ 图片渲染

本轮继续推进图片前端显示，但仍只限定在 SQLite 预览模式中，避免影响旧全局浏览和旧组卷逻辑。

已完成：

- SQLite 只读预览会识别 `\questionasset{alias}`；
- SQLite 编辑草稿预览也会识别 `\questionasset{alias}`；
- 已登记且本地存在的图片资源会直接以内联图片方式展示；
- 图片大于 2MB 或不是图片类型时，前端显示为附件提示，不强行 base64 嵌入；
- 未登记或文件缺失时，前端显示明确的缺失提示；
- SQLite 只读预览会识别旧 `\includegraphics{...}`；
- `includegraphics` 路径解析复用 `services.export_service.resolve_graphics_ref()`；
- 可解析且本地存在的旧图片会在 SQLite 预览里直接显示；
- 上述能力不修改 `utils/latex_ops.py`，因此不影响旧文件浏览、旧搜索预览、旧组卷预览。

验证：

```text
python -m py_compile question_bank_app.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_sqlite_inline_graphics
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- Python 编译通过；
- asset smoke 通过；
- question edit smoke 通过；
- database service smoke 通过；
- precommit database audit：`blockers=0`，`warnings=0`；
- UI layout detector 返回 `[]`。

当前进度估算：

- 数据底座和正式库：约 90%；
- 服务层查询能力：约 80%；
- 服务层编辑能力：约 72%；
- 图片资源服务：约 62%；
- 前端 SQLite 接入：约 63%；
- SQLite 导出能力：约 35%；
- 整体数据库重构计划：约 83%。

下一步建议：

1. 做 SQLite 新题录入草稿入口；
2. 做 AI/OCR 草稿审核页；
3. 做资源引用与题干/答案/解析字段之间的更严格一致性校验；
4. 做按试卷、教材、专题维度的 SQLite 导出；
5. 再逐步考虑 SQLite 结构替代旧文件浏览。

---

### 14.14 SQLite 手动新题录入草稿入口

本轮实现第一版 SQLite 手动录入入口。它不是正式替代旧“录入新题”，而是在旧入口顶部增加一个默认关闭的可选模式。

已完成：

- `services/import_service.py` 新增 `create_manual_entry_draft()`；
- 该服务会创建 `import_batch`；
- 该服务会写入 1 条 `question_import_draft`；
- 如果填写了图片/附件路径，会写入 `question_import_draft_asset`；
- 服务不会写入正式 `question` 表；
- 服务不会修改旧 `.tex` 文件；
- 新增 `scripts/smoke_manual_draft_service.py`；
- smoke 验证正式题目数不变，只增加临时库里的草稿数；
- “录入新题”页面新增 `🗃️ 使用 SQLite 草稿录入模式`；
- 该开关默认关闭，旧录入模式、旧 AI 识别、旧保存逻辑不受影响；
- 草稿录入表单支持来源类型、年份/册次、卷别代码、来源名称、题号/页码、知识板块/栏目、题型、难度、官方标记；
- 草稿录入表单支持题干、选项、答案、解析、标签、备注；
- 草稿录入表单支持图片/附件草稿路径，格式为 `role | 文件路径 | 说明`；
- 页面右侧提供当前草稿 TeX 下载和渲染预览；
- 页面右侧提供草稿状态概览。

重要边界：

- 默认保存为 `needs_review`；
- 只有用户勾选“字段完整且无警告时标记为 ready”时才可能进入 `ready`；
- 即使进入 `ready`，也只是草稿状态，不会自动进入正式题库；
- 后续仍需单独做草稿审核页与正式提交流程。

验证：

```text
python -m py_compile question_bank_app.py services/asset_service.py services/revision_service.py services/export_service.py services/import_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_manual_draft_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_manual_draft_entry
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- manual draft smoke 通过，且只写临时库；
- asset smoke 通过；
- question edit smoke 通过；
- database service smoke 通过；
- precommit database audit：`blockers=0`，`warnings=0`；
- UI layout detector 返回 `[]`。

当前进度估算：

- 数据底座和正式库：约 90%；
- 服务层查询能力：约 80%；
- 服务层编辑能力：约 74%；
- 图片资源服务：约 62%；
- 前端 SQLite 接入：约 68%；
- SQLite 草稿录入能力：约 45%；
- SQLite 导出能力：约 35%；
- 整体数据库重构计划：约 85%。

下一步建议：

1. 做 AI/OCR 草稿审核页；
2. 做草稿进入正式题库前的差异预览；
3. 做草稿确认入库的安全提交服务；
4. 做资源引用一致性校验；
5. 做按试卷、教材、专题维度的 SQLite 导出。

---

### 14.15 SQLite ???????????

?????????????????????????? SQLite ????????????????????? `.tex` ????????????????????????

????

- `services/import_service.py` ????????????????
- `commit_draft_to_question()` ?? `insert`?`update`?`skip` ???????
- `insert` ????? `Q000001` ??????? `question` ? `question_analysis`?
- `update` ???????????? `question_revision` ?????????
- `skip` ??????? `rejected`????????
- ????????? `committed`?????????????
- ??????????? `paper` ? `paper_question` ???
- ???????/????? `question_asset`???????????????
- ?????????? `question_revision`?
- ????/?????? `import_report_item`?
- `question_bank_app.py` ?????????????????
- ??????????????????????? `5/10/15/20`?
- ?????????????/???????????
- ????????????????TeX ??????????
- `update` ???????????????????
- ?? `scripts/smoke_draft_commit_service.py`???????????????????????????

?????

- `committed` ??????????????????????
- `ready` ? `approved` ???????
- ????????????????? `COMMIT`?
- ?? smoke ?????????????????????????
- ??????????????? TeX ????????????????

???

```text
python -m py_compile question_bank_app.py services/import_service.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_manual_draft_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_draft_commit_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_draft_commit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_draft_review_commit
python scripts/audit_question_assets.py --db data/mathcyclus.sqlite3
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

?????

- Python ?????
- draft commit smoke ??????????????????????????????
- manual draft smoke ??????????
- asset smoke ???
- question edit smoke ???
- database service smoke ???
- precommit database audit?`blockers=0`?`warnings=0`?
- question asset audit?`missing_includegraphics=0`?`unresolved_questionasset=0`?
- UI layout detector ?? `[]`?

???????

- ?????????? 90%?
- ????????? 82%?
- ????????? 78%?
- ???????? 64%?
- ?? SQLite ???? 73%?
- SQLite ???????? 65%?
- SQLite ???????? 55%?
- SQLite ?????? 35%?
- ??????????? 88%?

??????

1. ??????????????????????????????
2. ????????????????/?????????????
3. ?????? `\questionasset{...}` ?????????
4. ????????????? SQLite TeX ?????
5. ????? SQLite ?????????/????????

---

### 14.16 SQLite ??????????

???????????????????????????????????????????????????????????????????????????????????????????????????????????????

????

- `services/import_service.py` ?? `update_draft_question_fields()`?
- ??????????????????????????????????
- ??????????????????????
- ????????????????????/??????????/??/???????/??????????/???
- ??????? `normalized_tex`???????????????????
- ??????? `validation_json`?
- ?????????????????? `blocked`?
- ???? `ready/approved` ??????????????? `needs_review`?
- ?? `blocked/rejected` ???????????? `needs_review`??????????
- `committed` ???????????
- `commit_draft_to_question()` ?????????????????????? ready ????????
- `question_bank_app.py` ????????????????????????????????
- ???????????????????????
- ??????? `question_import_draft`?????? `question`??????? `.tex` ???
- `scripts/smoke_draft_commit_service.py` ????????????????????????????

???

```text
python -m py_compile question_bank_app.py services/import_service.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_manual_draft_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_draft_commit_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_draft_commit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_draft_field_edit
python scripts/audit_question_assets.py --db data/mathcyclus.sqlite3
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

?????

- Python ?????
- draft commit smoke ???????? `draft_field_edit_before_commit`?
- manual draft smoke ??????????
- asset smoke ???
- question edit smoke ???
- database service smoke ???
- precommit database audit?`blockers=0`?`warnings=0`?
- question asset audit?`missing_includegraphics=0`?`unresolved_questionasset=0`?
- UI layout detector ?? `[]`?

???????

- ?????????? 90%?
- ????????? 82%?
- ????????? 82%?
- ???????? 64%?
- ?? SQLite ???? 76%?
- SQLite ???????? 68%?
- SQLite ???????? 63%?
- SQLite ?????? 35%?
- ??????????? 89%?

??????

1. ????????????????????????????
2. ???????????????????/?????????????
3. ???????????????????????????????????????
4. ? SQLite ??????????? TeX?
5. ???? SQLite ????????????/??????????

---

### 14.17 SQLite ?????????

???????????????/?????????/???????????????????????????????????????????????????????

????

- `services/import_service.py` ?? `add_draft_asset()`?
- `services/import_service.py` ?? `update_draft_asset_fields()`?
- `services/import_service.py` ?? `delete_draft_asset()`?
- ?????? `problem/answer/solution/source/thumbnail` ?????
- ??????????????????????????????????
- ??/??????????? `mime_type`?
- ???????????? `file_hash`?
- ????????????????????????
- ??? `committed` ?????????????????
- `question_bank_app.py` ???????????/????????
- ???????????????????????????
- ???????????????????????????????????
- `scripts/smoke_manual_draft_service.py` ?????????????????
- ?? smoke ??????????????????? smoke ??????????????

?????

- ?????????? `question_import_draft_asset`?
- ??????????? `question_asset`?
- ?????????????????????/????? `question_asset`?
- ????????????????
- ??????? `\questionasset{...}` ?????????????????????????

???

```text
python -m py_compile question_bank_app.py services/import_service.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_manual_draft_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_draft_commit_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_draft_commit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_draft_asset_review
python scripts/audit_question_assets.py --db data/mathcyclus.sqlite3
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

?????

- Python ?????
- manual draft smoke ???????? `draft_asset_added_after_entry`?`draft_asset_updated_after_entry`?`draft_asset_deleted_after_entry`?
- draft commit smoke ???
- asset smoke ???
- question edit smoke ???
- database service smoke ???
- precommit database audit?`blockers=0`?`warnings=0`?
- question asset audit?`missing_includegraphics=0`?`unresolved_questionasset=0`?
- UI layout detector ?? `[]`?

???????

- ?????????? 90%?
- ????????? 82%?
- ????????? 84%?
- ???????? 68%?
- ?? SQLite ???? 78%?
- SQLite ???????? 70%?
- SQLite ???????? 68%?
- SQLite ?????? 35%?
- ??????????? 90%?

??????

1. ?????????????????????????????????
2. ????????????????????????????
3. ? SQLite ???/??/????? TeX ???
4. ? `scripts/` ? `reports/` ??????????????????? git???????????

---

### 14.18 ???????????

??????/????????????????????????????????????????????????????????????????????????????????????????????????????????????

????

- `services/asset_service.py` ?? `asset_aliases()`?
- `services/asset_service.py` ?? `find_questionasset_refs()`?
- `services/asset_service.py` ?? `find_includegraphics_refs()`?
- `services/asset_service.py` ?? `resolve_graphics_ref()`?
- `services/asset_service.py` ?? `resolve_asset_record_path()`?
- `services/asset_service.py` ?? `collect_asset_reference_issues()`?
- ?????????? `question_asset` ??? `question_import_draft_asset`?
- `\questionasset{alias}` ??? `asset_id/draft_asset_id/file_path/source_path/planned_file_path/original_file_name` ????? stem ???
- `\includegraphics{...}` ??? TeX ?????????????`chapters/` ?????????
- ???????? `includegraphics`???? `questionasset`?????????????? `questionasset` ???
- SQLite ???????????????????
- SQLite ????????????? / ????????????????????
- `scripts/audit_question_assets.py` ???????????????
- ??????? `draft_reference_blockers`??? `sample` ???????????
- `scripts/smoke_asset_service.py` ?????? `questionasset` ????? alias ???

?????

- ????????/?????????????????
- ??????????????????????? `source/thumbnail` ????????????????
- ????????????????????????? skipped ???
- ??????????? ready/approved ??????????? `questionasset` ??????????

???

```text
python -m py_compile question_bank_app.py services/import_service.py services/asset_service.py services/revision_service.py services/export_service.py scripts/smoke_manual_draft_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_draft_commit_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_draft_commit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_reference_consistency
python scripts/audit_question_assets.py --db data/mathcyclus.sqlite3 --stamp 20260902_reference_consistency_full
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

?????

- Python ?????
- asset smoke ????? `questionasset_reference_resolved` ? `questionasset_reference_missing_detected`?
- manual draft smoke ???
- draft commit smoke ???
- question edit smoke ???
- database service smoke ???
- precommit database audit?`blockers=0`?`warnings=0`?
- question asset audit?`missing_includegraphics=0`?`unresolved_questionasset=0`?`draft_reference_blockers=0`?
- UI layout detector ?? `[]`?

???????

- ?????????? 90%?
- ????????? 83%?
- ????????? 84%?
- ???????? 74%?
- ?? SQLite ???? 80%?
- SQLite ???????? 70%?
- SQLite ???????? 70%?
- SQLite ?????? 35%?
- ??????????? 91%?

??????

1. ????????????/??/??????????????
2. SQLite ??????????? TeX?
3. ?????????? ready/approved ????????????????
4. ??????????????? `reports/exports/data/assets` ??? git??????

---

### 14.19 ????????????????

??????????????????? SQLite ??????????????????????????????????????????????????????? SQLite ??????????????

????

- ?? `services/source_relation_service.py`?
- ??????????/???`upsert_question_paper_link()`?
- ???????????`delete_question_paper_link()`?
- ??????????/???`upsert_question_book_link()`?
- ???????????`delete_question_book_link()`?
- ??????????/???`upsert_question_topic_link()`?
- ???????????`delete_question_topic_link()`?
- ?????????????????? `question_revision`?
- ???????? `change_source=source_relation_edit`?
- ??????? `paper` ? `paper_question`?
- ??????? `book`?`book_section` ? `book_exercise_question`?
- ??????? `topic_module`?`topic` ? `topic_question`?
- `services/book_service.py` ??????????? `book_exercise_question_id`?
- `services/topic_service.py` ??????????? `topic_question_id`?
- ?? `scripts/smoke_source_relation_service.py`?
- smoke ?????????????/??/???????????????
- `question_bank_app.py` ???? SQLite ?????????????SQLite??????
- ??????????????????????????????
- ???????????????????????
- ?????????????????????

?????

- ?????? SQLite ????????? `.tex` ???
- ??????????????????????????
- ??????????????? AI ???????????????
- ??????????????? `paper/book/topic` ?????
- ???????????????????????????????????????????????

???

```text
python -m py_compile question_bank_app.py services/import_service.py services/asset_service.py services/revision_service.py services/export_service.py services/book_service.py services/paper_service.py services/topic_service.py services/source_relation_service.py scripts/smoke_manual_draft_service.py scripts/smoke_asset_service.py scripts/smoke_question_edit_service.py scripts/smoke_database_services.py scripts/smoke_draft_commit_service.py scripts/smoke_source_relation_service.py scripts/precommit_database_audit.py scripts/audit_question_assets.py scripts/export_db_to_tex.py
python scripts/smoke_source_relation_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_asset_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_manual_draft_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_draft_commit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_question_edit_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/precommit_database_audit.py --db data/mathcyclus.sqlite3 --stamp 20260902_source_relation_edit
python scripts/audit_question_assets.py --db data/mathcyclus.sqlite3 --stamp 20260902_source_relation_edit
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

?????

- Python ?????
- source relation smoke ???????/??/???????? revision ???
- asset smoke ???
- manual draft smoke ???
- draft commit smoke ???
- question edit smoke ???
- database service smoke ???
- precommit database audit?`blockers=0`?`warnings=0`?
- question asset audit?`missing_includegraphics=0`?`unresolved_questionasset=0`?`draft_reference_blockers=0`?
- UI layout detector ?? `[]`?

???????

- ?????????? 90%?
- ????????? 84%?
- ????????? 88%?
- ???????? 72%?
- ???????? 74%?
- ?? SQLite ???? 82%?
- SQLite ???????? 70%?
- SQLite ???????? 70%?
- SQLite ?????? 35%?
- ??????????? 92%?

??????

1. ???????????????????/??/????????
2. ? SQLite ????????????????? TeX?
3. ???????????????????????????
4. ????????? `.gitignore` ???

---

### 14.20 SQLite 按来源导出闭环

本轮目标是在不修改旧 `.tex` 题库、不改变旧全局浏览/组卷逻辑的前提下，让正式 SQLite 库可以按“试卷 / 教材 / 专题”来源关系反向导出为有序 TeX 文件。

已完成：

- `services/export_service.py` 增加来源导出服务层：
  - `list_source_export_options()`：列出可导出的试卷、教材、专题来源，并返回已关联题目数量；
  - `get_source_export_bundle()`：读取来源元信息、来源关系和完整题目，并按关系表排序；
  - `export_source_to_tex()`：把一个来源下的题目导出到单个 TeX 文件；
  - `export_paper_to_tex()`、`export_book_to_tex()`、`export_topic_to_tex()`：三类来源导出的包装入口；
  - `source_export_default_filename()`：生成稳定、可读、可保存的中文 TeX 文件名。
- 导出时会根据当前来源覆盖 legacy `problem` 头部元信息：
  - 试卷：覆盖年份、卷别、试卷名、题号和小题；
  - 教材：用 `BK` 作为来源类别，保留教材名、册次、页码、栏目、题号；
  - 专题：用 `TP` 作为来源类别，保留专题模块、专题名、分组和排序。
- 导出仍复用既有图片处理逻辑：
  - `\includegraphics{...}` 可选择复制到导出目录 `figures/`；
  - `\questionasset{alias}` 可选择解析为 `\includegraphics{figures/...}`；
  - 缺失图片不会静默吞掉，会进入返回结果和报告。
- `scripts/export_db_to_tex.py` 保留原有单题 / 批量导出参数，同时新增：
  - `--paper-id`；
  - `--book-id`；
  - `--book-section-id`；
  - `--topic-id`；
  - `--topic-group-name`；
  - `--source-kind` + `--source-id` 通用来源导出。
- `question_bank_app.py` 在 SQLite 浏览左侧筛选栏新增折叠入口 `SQLite TeX 导出`：
  - 支持选择试卷 / 教材 / 专题；
  - 支持选择具体来源；
  - 支持生成 TeX；
  - 支持直接下载刚生成的 TeX；
  - 仅写入 `exports/sqlite_source_exports/`，不修改正式数据库和旧 `.tex` 文件。
- 同一导出入口已支持“当前筛选结果合并导出”：
  - 可导出当前页；
  - 可导出当前筛选全部匹配；
  - 可导出当前筛选前 100 题或前 500 题；
  - 导出过程只读取 SQLite 文本字段，不进行 LaTeX 预览渲染，避免搜索后批量渲染带来的卡顿。
- 新增 `scripts/smoke_source_export_service.py`：
  - 从正式库读取来源；
  - 分别烟测试卷、教材、专题导出；
  - 额外烟测当前筛选合并导出；
  - 所有导出文件只写入系统临时目录；
  - 校验正式数据库题目数量不变。

验证：

```text
python -m py_compile question_bank_app.py services/export_service.py scripts/export_db_to_tex.py scripts/smoke_source_export_service.py
python scripts/smoke_source_export_service.py --db data/mathcyclus.sqlite3
python scripts/export_db_to_tex.py --db data/mathcyclus.sqlite3 --paper-id P54aba85b99 --stamp 20260902_source_export_smoke --resolve-questionassets
```

验证结果：

- Python 编译通过；
- source export smoke 通过；
- 正式库题目数量保持 `893`，导出过程不写库；
- 试卷、教材、专题三类来源均能生成包含 `\begin{problem}` 的 TeX 文件；
- 当前筛选合并导出可按 20 题上限生成 TeX，正式库不变。

当前进度估计：

- 数据库结构底座：90%；
- 旧题迁移与映射：84%；
- 前端 SQLite 浏览：88%；
- SQLite 编辑闭环：72%；
- 图片资源工作流：74%；
- 草稿录入与入库：82%；
- 来源关系维护：82%；
- SQLite TeX 导出：62%；
- 项目文件管理：92%。

后续优先级：

1. 把来源导出进一步细化为“按旧目录结构批量导出”；
2. 来源关系 UI 改为搜索选择已有试卷 / 教材 / 专题，减少手填 ID 和重复来源；
3. 图片引用一致性从 warning 逐步升级为 ready / approved 入库 gate；
4. 做一次 Git 文件清单审计，确认 `data/reports/exports/assets/questions` 的忽略规则没有误放行。

---

### 14.21 SQLite 预览布局修正与来源选择增强

本轮目标是修复 SQLite 全局浏览中出现的左侧栏拥挤、题型默认显示异常、右侧题卡边框层级过多的问题，并继续推进来源关系维护的可用性。

已完成：

- 修复 SQLite 左侧筛选栏布局：
  - 左侧吸附栏宽度从 `19rem` 调整为 `20.75rem`；
  - 降低左右栏间距，给左栏内部补充稳定节奏；
  - 修复分页、题目显示、导出折叠区在窄栏中挤压的问题。
- 修复题型筛选默认态：
  - 不再把 `None` 直接作为 Streamlit selectbox 选项；
  - 改用 `__all__` 作为 UI 哨兵值；
  - 传入查询服务前再转换回 `None`；
  - 解决“题型”处显示 `Choose an option` 的问题。
- 简化右侧题卡边框：
  - 收窄 CSS 选择器，避免 `:has(.mc-db-browse-card-anchor)` 套到上层容器；
  - 每道题只保留单层白色题卡；
  - `来源关系 / 图片资源` 折叠条改为轻量样式，减少额外框线。
- 来源关系管理增加“从已有来源填入”：
  - 试卷来源可从已有 `paper` 中选择并填入年份、卷别、文理/新高考、试卷名；
  - 教材来源可从已有 `book` 中选择并填入教材名、出版社、版本、年级、册次、课标；
  - 专题来源可从已有 `topic` 中选择并填入专题模块、专题名称、TeX 文件名；
  - 保存逻辑仍沿用原来的 `upsert_question_paper_link()`、`upsert_question_book_link()`、`upsert_question_topic_link()`，不改变旧 `.tex` 文件。
- 来源关系二级信息继续增强：
  - 教材来源选择已有教材后，可继续从 `book_section` 中选择章节并填入章节/栏目；
  - `services/topic_service.py` 新增 `list_topic_groups()`；
  - 专题来源选择已有专题后，可继续选择已有分组，并自动给出下一排序号；
  - `scripts/smoke_database_services.py` 增加 `topic_groups_query` 烟测。

验证：

```text
python -m py_compile question_bank_app.py services/export_service.py scripts/smoke_source_export_service.py
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/smoke_source_relation_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_source_export_service.py --db data/mathcyclus.sqlite3
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- Python 编译通过；
- database service smoke 通过；
- source relation smoke 通过；
- topic groups query 通过；
- source export smoke 通过；
- UI layout detector 返回 `[]`；
- 正式库题目数量仍为 `893`。

当前进度估计：

- 数据库结构底座：90%；
- 旧题迁移与映射：84%；
- 前端 SQLite 浏览：90%；
- SQLite 编辑闭环：74%；
- 图片资源工作流：74%；
- 草稿录入与入库：82%；
- 来源关系维护：88%；
- SQLite TeX 导出：62%；
- 项目文件管理：92%。

后续优先级：

1. 增加按旧目录结构批量导出；
2. 图片引用一致性从 warning 升级为入库 gate；
3. 来源关系 UI 后续再做搜索关键词过滤，避免来源数量继续增长后下拉过长；
4. 做一次提交前文件清单审计并准备 GitHub 版本。

---

### 14.22 SQLite 筛选栏精简与旧目录结构导出

本轮目标分两部分：先按用户截图修正 SQLite 左侧筛选栏的小交互歧义，再继续推进 TeX 导出体系中“按旧目录结构批量导出”的能力。

UI 修正：

- 精确搜索输入框原占位符 `题干 / 解答 / 标签 / 路径` 容易被误解为需要用斜杠分隔；
- 当前实际搜索逻辑是：输入一个关键词，系统同时在题干、答案/解析、标签、备注和文件路径中匹配；
- 已把说明改成“不需要用斜杠分隔”；
- 输入框占位符改成 `例如：函数、sin x、导数`；
- `知识板块` 与 `年份` 改为同一行两列显示，节省左侧栏纵向空间。

旧目录结构导出：

- 当前正式库中 `legacy_question_map.legacy_file_path` 基本是一题一个旧 `.tex` 路径；
- 因此本轮实现的是“镜像旧目录、一题一文件”导出；
- 输出目录只在 `exports/` 下生成，不覆盖 `chapters/`；
- 新增 `services/export_service.py`：
  - `safe_legacy_export_relative_path()`：把旧路径转为安全相对路径，异常路径落到 `unmapped/`；
  - `export_legacy_tree_to_tex()`：按筛选条件导出，并镜像 `chapters/.../*.tex`；
- 扩展 `scripts/export_db_to_tex.py`：
  - 新增 `--legacy-tree`；
  - 新增 `--keyword`、`--year`、`--chapter`、`--source`、`--question-type-id`、`--difficulty` 等批量筛选参数；
  - `--limit 0` 可用于导出匹配条件下全部题目；
- 扩展 `scripts/smoke_source_export_service.py`：
  - 增加 `legacy_tree_export_files`；
  - 增加 `legacy_tree_mirrors_chapters`；
  - 验证导出的文件路径包含旧 `chapters/...` 层级。

验证：

```text
python -m py_compile question_bank_app.py services/export_service.py scripts/export_db_to_tex.py scripts/smoke_source_export_service.py
python scripts/smoke_source_export_service.py --db data/mathcyclus.sqlite3
python scripts/export_db_to_tex.py --db data/mathcyclus.sqlite3 --legacy-tree --year 2026 --limit 3 --stamp 20260902_legacy_tree_smoke --resolve-questionassets
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- Python 编译通过；
- source export smoke 通过；
- legacy tree smoke 成功镜像生成 `chapters/三角函数/2026/...tex` 等路径；
- 正式库题目数量仍为 `893`；
- UI layout detector 返回 `[]`；
- CLI 测试导出产物位于被 `.gitignore` 覆盖的 `exports/` 和 `reports/`，不会进入提交。

当前进度估计：

- 数据库结构底座：90%；
- 旧题迁移与映射：84%；
- 前端 SQLite 浏览：91%；
- SQLite 编辑闭环：74%；
- 图片资源工作流：74%；
- 草稿录入与入库：82%；
- 来源关系维护：88%；
- SQLite TeX 导出：70%；
- 项目文件管理：92%。

后续优先级：

1. 图片引用一致性从 warning 升级为 ready / approved 入库 gate；
2. 来源关系 UI 增加关键词过滤，避免来源列表变长后选择困难；
3. 做提交前文件清单审计，准备下一版 GitHub。

---

### 14.23 SQLite 精确搜索、联动筛选与编辑态说明修正

本轮目标是修正 SQLite 浏览入口的三个高频交互问题：精确搜索分词不清楚、更多筛选下拉过大、单题进入编辑态后缺少清晰出口，同时补足导出入口的用途说明。

搜索逻辑：

- `services/question_db_service.py` 新增 `_split_keyword_terms()`；
- 精确搜索支持用 `/` 或 `／` 分隔多个关键词；
- 多个关键词之间采用 AND 逻辑：例如 `函数/导数` 表示两个词都要在题干、答案、解析、标签、备注、源码、旧路径、知识板块或来源字段中命中；
- 单个关键词仍保持原来的精确搜索入口，不改变旧筛选结构。

联动筛选：

- `list_question_filter_options()` 支持传入 `QuestionListFilters`；
- 不传筛选条件时仍返回全库选项，保持草稿录入、草稿审核等旧调用兼容；
- SQLite 浏览左侧先确定 `知识板块` 与 `年份`；
- `试卷来源` 下拉只展示当前知识板块与年份范围内出现过的来源；
- `题型` 下拉继续按当前知识板块、年份和来源收窄；
- `难度` 下拉继续按当前知识板块、年份、来源和题型收窄；
- 当前选项失效时自动回到“全部”，避免出现空白选择或查询错误。

编辑态：

- 单题卡片按钮改为 `开始修改 TeX`；
- 进入编辑态后左侧显示明确的 `TeX 编辑区` 说明；
- 编辑态右上角增加 `退出修改`，用于不保存退出；
- 保存按钮仍在编辑表单内，保存后只写 SQLite，并记录 revision；
- 旧 `.tex` 文件、旧全局浏览编辑、AI 解答、组卷逻辑均未改动。

导出说明：

- `SQLite TeX 导出` 改为 `SQLite TeX 导出（只生成文件）`；
- 面板内说明来源导出、筛选导出的差异；
- 明确所有导出只写入 `exports/`，不会修改 SQLite，也不会覆盖旧 `.tex` 题库；
- 筛选导出继续只拼接 TeX 源码，不触发大批量前端 LaTeX 渲染。

验证：

```text
python -m py_compile question_bank_app.py services/question_db_service.py scripts/smoke_database_services.py services/export_service.py scripts/smoke_source_export_service.py scripts/smoke_source_relation_service.py
python scripts/smoke_database_services.py --db data/mathcyclus.sqlite3 --limit 5
python scripts/smoke_source_relation_service.py --db data/mathcyclus.sqlite3
python scripts/smoke_source_export_service.py --db data/mathcyclus.sqlite3
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

验证结果：

- Python 编译通过；
- database service smoke 通过；
- `slash_keyword_search` 通过；
- `context_filter_sources` 通过；
- `context_filter_types` 通过；
- source relation smoke 通过，且不写正式库；
- source export smoke 通过，且不写正式库；
- UI layout detector 返回 `[]`；
- 正式库题目数量仍为 `893`。

当前进度估计：

- 数据库结构底座：90%；
- 旧题迁移与映射：84%；
- 前端 SQLite 浏览：93%；
- SQLite 编辑闭环：77%；
- 图片资源工作流：74%；
- 草稿录入与入库：82%；
- 来源关系维护：88%；
- SQLite TeX 导出：72%；
- 项目文件管理：92%。

后续优先级：

1. 做一次 SQLite 编辑态实机点选验证，重点看 `开始修改 TeX`、`退出修改`、`保存修改` 的前端手感；
2. 图片引用一致性从 warning 升级为 ready / approved 入库 gate；
3. 来源关系 UI 增加关键词过滤，避免来源列表继续增长后选择困难；
4. 做提交前文件清单审计，准备下一版 GitHub。

---

### 14.24 多来源关系、TeX 格式分离与结构化选项编辑规划

本轮把后续 SQLite 主存储形态的三个关键设计固定下来：复杂来源关系如何看、SQLite 内部源码与旧 TeX 导出如何分离、选择题选项如何避免手写 `\choice{{...}}` 出错。

#### 14.24.1 多来源关系模型

未来同一道题可能同时出现在：

- 某一年高考卷；
- 某本教材的某页、某栏目；
- 某次模考卷；
- 某个专题或讲义文件。

因此题目本体不能再假设只有一个“来源”。推荐长期模型是：

- `question` 只保存题目本体：题干、选项、答案、解析、题型、难度、标签、备注、标准化状态；
- `paper`、`book`、`topic` 等表保存来源实体；
- `paper_question`、`book_exercise_question`、`topic_question` 等关系表保存“题目在某个来源中的位置”；
- 每条来源关系至少记录：
  - 来源类型：试卷 / 教材 / 专题 / 其它；
  - 来源 ID；
  - 年份、卷别、文理/新高考、试卷名；
  - 教材名、章节、页码、栏目、题号、小题；
  - 专题模块、专题名、分组、排序；
  - 是否主来源；
  - 关系置信度；
  - 备注；
  - 创建时间与更新时间。

前端展示原则：

- 题卡顶部只显示一个“主来源摘要”，用于快速识别；
- 复杂来源关系不再堆在题卡底部；
- 未来改为右侧悬浮 `题目资料` 抽屉；
- `题目资料` 抽屉建议分为三个页签：
  - `来源关系`：显示所有试卷、教材、专题、其它来源；
  - `图片资源`：显示图片/PDF 资源与可插入占位符；
  - `修订记录`：显示 revision 历史；
- 当一道题有多个来源时，在题卡顶部显示轻量提示，例如 `另有 2 个来源`，点击后打开右侧抽屉。

#### 14.24.2 SQLite 源码与 TeX 导出格式分离

SQLite 成为主存储后，旧 `.tex` 文件开头的：

```tex
% === Begin Label Data ===
% ID: ...
% 难度星级:
% 标签:
% 备注:
% 组卷引用次数:
% === End Label Data ===
```

不应再作为 SQLite 内部题目源码的一部分。

推荐长期规则：

- SQLite 内部只保存结构化字段，不把 `Label Data` 混入 `stem_tex`、`answer_tex`、`solution_tex`；
- `Label Data` 只在“兼容旧格式导出”时由导出器生成；
- 前端展示的 `TeX 源码` 分两种模式：
  - `结构化源码`：只展示题干、选项、答案、解析等内容字段；
  - `兼容导出预览`：展示会导出的完整旧格式 TeX；
- 后续 `problem` 命令如果扩展参数，也由导出器负责生成，不直接污染数据库字段；
- 所有旧 `.tex` 文件继续保留为迁移来源和回退备份，不作为未来主数据入口。

这意味着长期架构应该是：

```text
SQLite 结构化题目
    -> 旧题库兼容导出器：Label Data + problem + answer + solutions
    -> 讲义 problem 导出器：未来新版 problem 命令
    -> 试卷排版导出器：按试卷模板生成
    -> 前端预览器：结构化字段实时渲染
```

#### 14.24.3 结构化选项编辑器

当前旧题库要求选择题选项导出为：

```tex
\begin{choices}
\choice{{$\dfrac{\pi}{4}$}}
\choice{{$\dfrac{\pi}{2}$}}
\end{choices}
```

用户不应在编辑器里手写 `\choice{{...}}`。推荐长期规则：

- 数据库内部存选项数组；
- 前端只让用户填写“选项内部 TeX”；
- 导出器自动生成 `\choice{{...}}` 结构；
- 编辑器提供选项数量控制，至少支持 2～8 个选项；
- 常用选择题默认 4 个选项；
- 每个选项给出 A/B/C/D 编号；
- 用户填写示例：`$\dfrac{\pi}{4}$`；
- 保存或导出时自动转换为 `\choice{{$\dfrac{\pi}{4}$}}`；
- 如果用户粘贴了 `{...}` 形式，兼容层应避免重复包裹成 `{{...}}` 之外的异常层级。

分阶段实现建议：

1. **兼容层阶段**：不改数据库结构，前端把用户填写的内部 TeX 转为现有 `choices_json` 兼容形态；
2. **导出器规范阶段**：新增明确的 `format_choice_item()`，统一处理是否补外层花括号；
3. **数据库净化阶段**：把 `choices_json` 中历史遗留的 `{...}` 内容迁移为纯内部 TeX；
4. **前端正式阶段**：SQLite 编辑、草稿录入、AI/OCR 草稿审核都统一使用结构化选项编辑器；
5. **旧格式导出阶段**：只有导出器负责恢复 `\choice{{...}}`。

当前实现原则：

- 短期不直接迁移历史 `choices_json`；
- 短期不改变旧 `.tex` 导出结果；
- 先在 SQLite 单题编辑态、草稿录入和草稿审核做前端兼容层，验证手感；
- 后续再考虑历史数据净化和新版 `problem` 命令导出。

当前进度估计：

- 数据库结构底座：90%；
- 旧题迁移与映射：84%；
- 前端 SQLite 浏览：95%；
- SQLite 编辑闭环：84%；
- 图片资源工作流：78%；
- 草稿录入与入库：84%；
- 来源关系维护：91%；
- SQLite TeX 导出：72%；
- 项目文件管理：92%。

后续优先级：

1. 继续收敛右侧 `题目资料` 抽屉的信息密度与可编辑入口；
2. 图片引用一致性从 warning 升级为 ready / approved 入库 gate；
3. 设计来源关系搜索选择器，减少手填导致的重复来源；
4. 把新版结构化选项方案从兼容层推进到历史数据净化；
5. 提交前做项目文件清单审计。

#### 14.24.4 本轮已执行的 UI 收敛

当前已经完成第一版轻量落地：

- SQLite 单题编辑态不再使用“每行一个选项”的原始 textarea；
- 选项编辑区改为“选项数量 + `+` / `−` 调整 + A/B/C/D 两列输入框”；
- SQLite 草稿录入和草稿审核也改为结构化选项编辑区；
- 草稿表单位于 `st.form` 内，不使用普通 `st.button`，但保留数量输入和 A/B/C/D 两列输入框；
- 用户仍只填写选项内部 TeX；
- 保存前继续自动转成当前兼容形态 `{...}`，不改变现有数据库结构；
- 导出器继续统一输出 `\choice{{...}}`；
- SQLite 只读题卡底部不再额外堆叠 `来源关系 / 图片资源`；
- 只读题卡改为右侧轻量 `题目资料` 下拉，集中展示：
  - 来源关系；
  - 图片资源；
  - 图片引用检查；
- 编辑态仍保留完整的图片资源管理和来源关系管理入口，保证现有功能不丢。

这一阶段仍然没有做的事：

- 没有迁移历史 `choices_json`；
- 没有修改旧 `.tex` 文件；
- 没有改变 AI 改卷、旧全局浏览、组卷服务的基本逻辑；
- 没有把修订记录强行加载到只读题卡，避免增加每页批量查询负担。

#### 14.24.5 图片引用入库门禁

为了避免“题面里已经写了图片引用，但资源并未登记或文件缺失”的草稿进入正式库，本轮把图片门禁从前端提示下沉到服务层。

门禁规则：

- `\includegraphics{...}` 找不到实际文件：阻塞；
- `\questionasset{...}` 找不到登记资源：阻塞；
- 已登记资源文件缺失：阻塞；
- 已登记但未被引用：警告，不阻塞；
- 草稿只有在校验结果为 `ready` 时才允许继续标记为 `ready/approved` 并提交正式库；
- `commit_draft_to_question` 再做一次最终拦截，防止绕过 UI 直接入库。

实现结果：

- 手动草稿如果带缺失图片引用，会直接落为 `blocked`；
- `ready / approved` 状态不会再接受带门禁错误的草稿；
- 正式提交前会再次校验图片引用，避免只靠前端状态控制。

#### 14.24.6 来源关系搜索选择器

当前编辑态的来源关系管理已经从“纯手填”推进到“可搜索填入”：

- 试卷、教材、专题三类已有来源都新增搜索框；
- 搜索支持用 `/` 分隔多个关键词；
- 结果会按当前关键词过滤，再用下拉选择要填入的来源；
- 搜索不到结果时仍可直接手动填写，不会堵死流程；
- 这一层只改前端 UI，不改来源表结构、不改 upsert 逻辑。

这样做的目的不是替换手工录入，而是减少重复造来源，避免同一个试卷/教材/专题被反复手填出多个近似名字。

#### 14.24.7 图片插入入口前置

当前编辑态的图片入口做了一个低风险优化：

- 把“图片插入 / 登记资源”入口前置到 `TeX 编辑区` 顶部；
- 仍保留下方的资源列表和 `\questionasset{...}` 插入按钮；
- 资源登记逻辑、文件复制逻辑、占位符生成逻辑都不变；
- 只是把最常用的入口从页面底部挪到了更容易触达的位置。

这样做的目标是减少编辑时滚动距离，让“先登记资源、再插入占位符”这条路径更短。

#### 14.24.8 登记后直插目标字段

图片入口再往前推进一步：

- 登记图片资源时可直接选择插入到 `题干 TeX`、`答案 TeX`、`解析 TeX`，或只登记不插入；
- 该动作只是在前端把 `\questionasset{...}` 追加到目标字段；
- 图片上传、复制、登记、占位符生成的底层逻辑不变；
- 这样能减少“登记完再去找占位符复制”的手工步骤。

---

## 15. 2026-09-02 当前收口状态

### 15.1 当前进度判断

SQLite 重构第一版当前估算约 `95%`。

已经完成的主线能力：

- 正式 SQLite 数据库已建立，当前正式库为 `data/mathcyclus.sqlite3`；
- 题目、试卷、知识板块、图片资源、教材关系、专题关系均进入结构化表；
- 全局浏览旁路新增 `SQLite预览` 入口；
- SQLite 预览页支持左侧吸附筛选、年份/板块/来源/题型/难度联动筛选、精确搜索、分页和单题选择；
- 单题编辑支持只显示当前题、三列编辑、草稿预览、保存写入 SQLite、revision 记录、退出后回到原列表位置；
- 非 TikZ 图片支持 `\questionasset{alias}` 占位符、前端预览、资源登记、审计和导出解析；
- 试卷/教材/专题来源关系服务与前端维护入口已具备；
- SQLite 导出器支持按来源导出、按筛选导出、按旧 `chapters/` 目录结构导出；
- 发布前总检查脚本 `scripts/release_readiness.py` 已建立。

### 15.2 当前不要急着做的事情

为了避免破坏已经稳定的旧功能，以下动作应放到下一阶段再做：

- 不直接把旧全局浏览改成只读 SQLite 数据源；
- 不直接把旧组卷服务改成 SQLite 数据源；
- 不删除旧 `.tex` 题库；
- 不把正式 SQLite、预览 SQLite、报告 JSON、导出文件、题目图片资产直接提交到 GitHub；
- 不在没有实机 UI 确认前大幅重排主程序页面。

### 15.3 当前推荐命令

查看当前重构摘要：

```text
python scripts/rebuild_status.py --brief
```

运行发布前总检查：

```text
python scripts/release_readiness.py
```

当前发布前总检查结果口径：

- `failed=0`：可以继续准备提交；
- `warnings=1`：主要是 `question_bank_app.py` 作为主程序有真实内容改动，提交前需要人工确认；
- 正式数据库审计、图片资源审计和核心 smoke 均已通过。

### 15.4 下一阶段推荐顺序

后续建议按以下顺序继续：

1. 实机确认 SQLite 预览页的筛选、搜索、单题编辑、图片显示、来源关系管理是否满足日常使用；
2. 整理本轮新增脚本、服务、文档、模板，确认哪些进入 GitHub；
3. 推送一版以“SQLite 第一版旁路可用”为目标的提交；
4. 再单独开下一阶段：让旧全局浏览逐步读取 SQLite，但保留回退入口；
5. 最后再考虑组卷服务数据库化，不建议和旧全局浏览迁移混在同一轮做。

### 15.5 2026-09-02 发布检查补充

本轮继续把收尾动作往“可提交、可回看、可复现”推进。

已完成：

- `scripts/release_readiness.py` 已通过语法检查；
- 发布前总检查已重新执行，结果为 `status=warning`、`failed=0`、`warnings=1`；
- 唯一 warning 仍来自 `scripts/check_project_hygiene.py`：主程序 `question_bank_app.py` 存在真实内容改动，属于提交前人工确认项，不是功能阻断；
- readiness Markdown 报告已经补充 `失败 / 警告` 计数、warning marker、fail marker 和异常命令尾部输出；
- 当前正式库摘要仍为：题目 893 条、试卷 302 条、试卷题目关系 888 条、图片资源 7 条、教材关系 1 条、专题关系 3 条；
- 当前整体进度维持约 `95%`，状态是“SQLite 第一版旁路可用，等待实机确认与提交文件集确认”。

本轮最新验证命令：

```text
python -m py_compile scripts/release_readiness.py
python scripts/release_readiness.py
python scripts/rebuild_status.py --brief
```

最新报告位置：

```text
reports/release_readiness_20260902_222603.md
reports/release_readiness_20260902_222603.json
```

下一步不建议直接做大迁移。更稳妥的顺序是：

1. 先确认 GitHub 本轮提交文件集；
2. 推送“SQLite 第一版旁路可用”版本；
3. 再开新阶段迁移旧全局浏览数据源；
4. 最后迁移组卷服务数据源。

### 15.6 2026-09-02 录入问题入口补强

本轮把新版 SQLite 草稿录入从旧“录入新题”里的隐藏 checkbox 进一步前置为独立入口。

已完成：

- 左侧主导航新增 `录入问题`；
- 旧 `录入新题` 仍保留原有 `.tex`、OCR、批量文件写入、同卷写入和挖空题逻辑；
- `录入问题` 渲染 SQLite 草稿录入工作台，写入目标仍是 `question_import_draft`；
- 草稿录入工作台分成左侧模式栏、中间录入表单、右侧实时预览；
- 左侧模式栏新增四种录入方式：
  - 单题录入；
  - 批量试题录入；
  - 同卷试题录入；
  - 同书试题录入；
- 单题录入保留结构化选项编辑，并改为一行三个选项输入框；
- 批量、同卷、同书模式支持粘贴多题 TeX，优先识别 `---xxx.tex---` 分隔符，其次按多个 `problem` 环境拆分；
- 新增服务层 `create_manual_entry_drafts()`，让多题录入共享同一个 import batch；
- 更新手动草稿 smoke，覆盖批量草稿保存；
- 规范说明中新增 SQLite 录入问题规则。

验证结果：

```text
python -m py_compile question_bank_app.py services/import_service.py scripts/smoke_manual_draft_service.py
python scripts/smoke_manual_draft_service.py
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
python scripts/release_readiness.py
```

结果：

```text
smoke_manual_draft_service: status=ok, writes_formal_database=false
layout detector=[]
release_readiness: status=warning, failed=0, warnings=1
```

当前整体进度可从约 `95%` 提升到约 `96%`。剩余主要不是数据库底座能力，而是：

1. 实机确认新 `录入问题` 页的字段密度、预览位置和四种模式是否顺手；
2. 决定旧 `录入新题` 里的 SQLite checkbox 是否保留为过渡入口；
3. 后续再开始旧全局浏览 / 组卷服务的数据源迁移。

### 15.7 录入问题页第二轮纠偏

用户实机确认后，第一版 `录入问题` 的问题是“太像草稿审核后台”，不够像原本的新题录入工作台。本轮将设计口径明确为：

- `录入新题`：旧 `.tex` / OCR / 批量文件写入 / 同卷文件写入 / 挖空题生成入口；
- `录入问题`：新版 SQLite 草稿录入入口；
- 两者并列，不在旧 `录入新题` 内继续隐藏 SQLite checkbox。

本轮调整：

- 左侧主栏保留四种录入方式；
- 左侧主栏下方恢复 `AI 图片 / PDF 识别`，与旧录入页工作流保持一致；
- 移除左侧 `草稿状态`，避免干扰录入任务；
- 单题录入中间列恢复接近旧 `单题详情` 的结构；
- 小题改为可选开关，默认不显示；
- 来源标签改为系统自动生成，不再让用户录入；
- 单题默认就是新增题，不再显示 `入库动作`；
- 目标 `question_id` 由后续入库流程分配，不再显示手填框；
- `官方题 / 官方来源` 暂时不显示，等来源关系模型更稳定后再决定是否恢复；
- 单题模式不再默认显示 `草稿审核与确认入库`；
- 批量、同卷、同书模式仍保留审核与确认入库，因为整卷/整书录入更需要批量校验；
- 右侧预览移除草稿 TeX 下载，改为生成完整题图、下载题图和浏览器允许时复制题图。

关键解释：

- 单题选项输入的是内部 TeX；
- 系统在预览/保存时自动把选项拼入 `problem` 环境；
- 选项位置为题干之后、`\end{problem}` 之前；
- 这符合后续 `problem` 命令兼容方向。

最新验证：

```text
python scripts/release_readiness.py
```

结果：

```text
status=warning
failed=0
warnings=1
```

唯一 warning 仍是主程序存在真实内容改动，需要提交前人工确认。

### 15.8 2026-09-02 录入问题页第三轮微调

本轮继续收紧 `录入问题` 的单题模式布局，目标是让它更像一个可持续录题的工作台，而不是审稿后台。

已完成：

- 左中右三栏比例改为更偏中间录入区的布局，降低左栏压迫感；
- 左侧选项区标题改为“选项 + 说明”同一行，下面单独一行显示“选项数量”；
- 单题模式的 `答案 TeX` 与 `解析 TeX` 改为各自独立一行，不再并排；
- `图片 / 附件草稿路径` 改为默认收起的开关，减少底部白块干扰；
- 右侧预览增加“参考保存文件名”，便于录题时直接确认最终落盘命名；
- 右侧预览改为三段式展示：题干、答案、解答；
- 预览仍复用现有 TeX 转 Markdown 和题图生成链路，没有改动正式入库逻辑；
- 结构化选项输入仍保持内部 TeX 录入，保存时自动包装为 `\\choice{{...}}`。

验证：

```text
python -m py_compile question_bank_app.py
python scripts/smoke_manual_draft_service.py
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

结果：

```text
py_compile: ok
smoke_manual_draft_service: status=ok
layout detector: []
```

当前判断：SQLite 草稿录入入口已经从“功能可用”推进到“字段和预览节奏可继续打磨”的阶段，下一步更适合继续微调单题模式的来源关系与图片插入体验，而不是再扩新功能。

### 15.9 2026-09-03 图片占位符复制与来源关系密度优化

本轮继续围绕“单题编辑里的来源关系与图片插入体验”推进，不改变旧 `.tex` 题库、不改变旧 AI 改卷/旧全局浏览/旧组卷的基本逻辑，只补 SQLite 预览与编辑工作流里的前端呈现。

已完成：

- `\questionasset{...}` 的默认占位符仍保持“文件名主干”规则，不改成 caption，确保导出、smoke 和既有占位符预期一致；
- `caption`/“命名”继续作为可解析别名和界面展示信息保留，方便人工管理；
- 新增通用文本复制按钮，用于复制 `\questionasset{...}`；
- SQLite 单题编辑的图片上传卡片中增加“复制占位符”，上传前也能把当前引用名对应的占位符复制到题干、答案或解析；
- SQLite 正式资源管理区增加“复制占位符”，已经登记入库的图片资源可以一键复制引用；
- 题目预览里的图片/附件卡片增加复制入口，降低人工插入图片占位符的操作成本；
- 来源关系管理区顶部统计改为更紧凑的摘要样式；
- 来源关系管理中的“搜索已有来源 / 匹配结果 / 填入”压缩为单行布局；
- 教材章节与专题分组的“选择 / 填入”也压缩为单行布局；
- 三类来源关系的增删改逻辑保持不变，仍通过 SQLite 服务层写入并记录 revision。

验证：

```text
python -m py_compile question_bank_app.py services/asset_service.py services/export_service.py
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
```

结果：

```text
py_compile: ok
layout detector: []
```

当前判断：

- SQLite 图片资源链路已经从“能登记、能引用、能导出”推进到“录入时可复制、编辑时可复制、预览时可识别”的状态；
- 来源关系管理仍是功能完整但信息量偏大的高级区块，后续若继续优化，更适合把它做成右侧抽屉或单题详情里的独立浮层；
- 整体数据库重构进度仍按约 `96%` 估算，目前剩余重点是旧全局浏览 / 组卷服务向 SQLite 数据源迁移，以及实机确认新录入入口的长期使用手感。

### 15.10 2026-09-03 单题编辑工作台右侧管理入口

本轮把上一节提到的“来源关系管理适合右侧抽屉或独立浮层”先落成 Streamlit 当前能力范围内最稳的方案：在单题 TeX 编辑工作台的右侧预览列顶部增加管理入口，避免页面底部继续堆叠高密度表单。

已完成：

- 单题编辑工作台右侧新增 `图片资源 · N` 入口；
- 单题编辑工作台右侧新增 `来源关系 · N` 入口；
- `来源关系 · N` 内部直接显示试卷、教材、专题三类数量；
- 原底部的图片资源管理与来源关系管理不再直接展开堆在页面末端；
- `来源关系管理（SQLite）` 渲染函数支持无外层 expander 嵌入，因此同一套表单既能独立使用，也能放入右侧管理入口；
- 新增轻量计数函数 `_db_preview_source_relation_counts()`，只为右侧入口显示当前关系数量，不改变服务层写入逻辑；
- 图片、来源关系的保存、移除、填入、revision 记录仍复用原有服务层函数。

设计判断：

- 当前先采用 `st.popover`，因为它能承载现有 Streamlit 表单和按钮，比纯 HTML 浮层更稳；
- 真正类似组卷篮的固定右侧浮层如果要承载可提交表单，需要后续重构为组件化方案，不能简单用 HTML/CSS 包住现有 Streamlit widgets；
- 这一步优先解决“底部信息太重、编辑时视线割裂”的问题。

验证：

```text
python -m py_compile question_bank_app.py
python -m py_compile question_bank_app.py services/asset_service.py services/export_service.py services/source_relation_service.py services/import_service.py
python scripts/smoke_manual_draft_service.py
python scripts/smoke_asset_service.py
python scripts/smoke_source_relation_service.py
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
python scripts/release_readiness.py
```

结果：

```text
py_compile: ok
smoke_manual_draft_service: status=ok, writes_formal_database=false
smoke_asset_service: status=ok, writes_formal_database=false
smoke_source_relation_service: status=ok, writes_formal_database=false
layout detector: []
release_readiness: status=warning, failed=0, warnings=1
```

说明：`release_readiness` 的唯一 warning 仍是当前工作区存在真实改动，需要提交前人工确认，不是测试失败。

当前整体进度仍按约 `96%` 估算。SQLite 单题浏览、编辑、图片引用、来源关系维护已经基本形成闭环；下一阶段更适合推进旧全局浏览/组卷服务的数据源迁移准备，或者先做一次实机 UI 回归。

### 15.11 2026-09-03 SQLite 到旧浏览/组卷字段的只读适配层

本轮在不接管旧全局浏览和旧组卷服务的前提下，先补了一个只读适配层，为后续把旧页面数据源迁到 SQLite 做准备。

已完成：

- 新增 `services/sqlite_legacy_adapter.py`；
- 将 SQLite `question bundle` 转换为旧 CSV 索引兼容字段：
  - `题目ID`；
  - `文件名称`；
  - `相对文件路径`；
  - `年份`；
  - `试卷类型`；
  - `试卷名称`；
  - `原卷题号`；
  - `知识板块`；
  - `标签`；
  - `包含TikZ绘图`；
  - `题型`；
  - `难度星级`；
  - `包含解析`；
  - `组卷引用次数`；
  - `备注`；
  - `题干`；
  - `答案`；
  - `解析`；
- 额外保留 `SQLite题目ID` 和 `legacy_tex`，方便未来旧题卡直接读取渲染内容；
- 新增 `sqlite_bundle_to_legacy_card()`，输出未来旧题卡/组卷卡可用的 `label/content/file/path/subject/row/source` 结构；
- 新增 `list_sqlite_legacy_cards()`，支持按 `QuestionListFilters` 分页读取 SQLite 题目并转成旧卡片 payload；
- 新增 `scripts/smoke_sqlite_legacy_adapter.py`；
- 将 `smoke_sqlite_legacy_adapter` 纳入 `scripts/release_readiness.py` 的慢速发布检查。

验证：

```text
python -m py_compile services/sqlite_legacy_adapter.py scripts/smoke_sqlite_legacy_adapter.py scripts/release_readiness.py question_bank_app.py
python scripts/smoke_sqlite_legacy_adapter.py
python scripts/release_readiness.py
```

结果：

```text
smoke_sqlite_legacy_adapter: status=ok, writes_formal_database=false
release_readiness: status=warning, failed=0, warnings=1
```

说明：

- 这一步没有替换旧页面，只建立迁移前的只读桥；
- 后续可以先用这个桥在一个隐藏/可选的 SQLite 数据源模式里试跑旧题卡和旧组卷篮；
- 若稳定，再逐步把旧全局浏览与旧组卷服务切到 SQLite，而不是一次性硬替换。

当前整体进度可从约 `96%` 提升到约 `97%`。剩余主要工作：

1. 旧全局浏览的数据源切换方案；
2. 旧组卷服务的数据源切换方案；
3. SQLite 题卡与旧题卡行为差异核对；
4. 实机确认右侧管理入口、图片占位符复制和录入问题页长期使用手感。

### 15.12 2026-09-03 SQLite 试用选题接入旧组卷篮

本轮开始把上一节的只读桥接能力接到真实前端入口，但仍保持“旧全局浏览 / 旧组卷服务默认不被替换”的原则。也就是说，默认选题仍走旧 `chapters/` 与 CSV 索引；SQLite 入口先作为可选试用源，验证筛选、预览、加入试题篮和排版工作台的兼容性。

已完成：

- `services/sqlite_legacy_adapter.py` 新增 `resolve_legacy_card_file_path()`；
- 该函数只允许解析项目根目录内实际存在的 `.tex` 文件，避免 `sqlite://...` 或外部路径被送入旧组卷流程；
- `scripts/smoke_sqlite_legacy_adapter.py` 增加旧路径解析检查；
- 组卷题卡的“加入 / 移出组卷”状态切换提取为共享函数 `_toggle_exam_selection_path()`；
- 共享函数会同步 `exam_selected_qs`、`exam_expanded_q`、右侧试题篮展开状态，以及已存在的 `exam_blocks` 顺序；
- SQLite 预览题卡新增“加入试题篮 / 移出试题篮”按钮；
- SQLite 预览侧栏新增“加入当前页可组卷题”，只处理当前分页内的题目，避免一次性批量渲染大量内容；
- `services/sqlite_legacy_adapter.py` 新增 `list_resolved_legacy_card_paths()`，用于批量动作只读取摘要路径，不提前加载完整题目 bundle；
- 新增 `scripts/audit_sqlite_legacy_bridge.py`，全库审计 SQLite 题目是否都能解析到旧 `.tex` 文件，并抽样检查题卡/旧文件是否具备 `problem` 环境；
- 组卷服务选题阶段新增“选题来源”切换：
  - 默认：`旧 TeX 题库`；
  - 试用：`SQLite 试用题库`；
- 选择 `SQLite 试用题库` 时复用 SQLite 预览的筛选、分页和渲染，但禁用单题编辑与 TeX 导出面板；
- SQLite 试用选题会把可解析到旧文件的题目加入现有右侧悬浮试题篮；
- 组卷服务的 SQLite 试用模式新增“随机补足至 N 题”，从当前 SQLite 筛选条件下随机补足到上方组卷数量，最多读取候选摘要，不触发大批量 LaTeX 渲染；
- 后续排版工作台仍沿用旧组卷生成逻辑，避免一次性重写排版、模板、PDF 编译链路。

边界说明：

- 这一步不是最终迁移，只是“SQLite 作为选题索引，旧 `.tex` 文件作为排版载体”的过渡形态；
- SQLite 数据库在此入口保持只读；
- 最终生成试卷时，旧组卷的“组卷引用次数”逻辑仍可能更新 `chapters/` 内对应旧题文件；
- 若某个 SQLite 题目没有可复用的旧 `.tex` 路径，按钮会禁用，避免出现“加入了但无法生成”的状态；
- AI 辅助预组卷暂时仍限定旧 `CSV + chapters` 数据源，SQLite 自动抽题需要后续单独接 `QuestionListFilters` 和难度/题型抽样逻辑。

验证：

```text
python -m py_compile question_bank_app.py services/sqlite_legacy_adapter.py scripts/smoke_sqlite_legacy_adapter.py
python scripts/smoke_sqlite_legacy_adapter.py
python scripts/audit_sqlite_legacy_bridge.py
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json --scope layout question_bank_app.py
python scripts/release_readiness.py
```

结果：

```text
py_compile: ok
smoke_sqlite_legacy_adapter: status=ok, writes_formal_database=false
legacy_card_paths_resolve_for_page: ok
legacy_summary_paths_resolve_for_page: ok
source_database_unchanged: ok
audit_sqlite_legacy_bridge: status=ok, scanned=893, resolved_paths=893, unresolved=0
layout detector: []
release_readiness: status=warning, failed=0, warnings=1
```

说明：`release_readiness` 的唯一 warning 仍来自项目卫生检查提示当前工作区存在真实改动，需要提交前人工确认；所有编译、数据库审计、资源审计和 smoke 均通过。

当前整体进度可从约 `97%` 提升到约 `98%`。剩余主要工作：

1. SQLite 试用选题的实机 UI 回归；
2. 旧全局浏览是否需要增加同类“SQLite 试用题卡”模式；
3. SQLite AI 自动抽题候选池与旧 AI 辅助预组卷的规则对齐；
4. 最终迁移前的旧题卡/SQLite 题卡行为差异清单；
5. 发布前完整回归与文件清单确认。

### 15.13 2026-09-03 SQLite AI 自动抽题候选池

本轮继续推进 15.12 留下的第三项：让 `SQLite 试用题库` 不只是手动筛选和随机补足，而是可以进入一套与旧 AI 辅助预组卷规则对齐的自动抽题候选池。旧 `CSV + chapters` 的默认路径仍不替换，旧页面逻辑仍保留。

已完成：

- 新增 `services/exam_selection_service.py`；
- 将自动抽题核心规则沉到无副作用服务层：
  - `高考范围` 展开为既定高考知识板块；
  - 非试卷模板按目标难度附近的基础 / 中档 / 难题比例抽取；
  - 试卷类模板按新高考结构尽量抽取 19 题；
  - 支持组卷意图关键词打分；
  - 支持“最后一道 / 压轴”类意图对末端解答题的板块偏好；
  - 支持多选题软识别；
  - 支持按组卷引用次数降低重复使用倾向；
  - 输出只包含行数据，服务层不写 UI 状态、不写 SQLite、不写旧 `.tex`；
- `services/question_db_service.py` 的题目列表摘要增加 `stem_tex / answer_tex / solution_tex / created_at / updated_at` 字段，供候选池打分使用；
- `services/sqlite_legacy_adapter.py` 新增 `sqlite_summary_to_legacy_row()`；
- `services/sqlite_legacy_adapter.py` 新增 `list_sqlite_legacy_rows()`，支持分页或全量扫描 SQLite 摘要并转为旧 CSV row 形状；
- `sqlite_summary_to_legacy_row()` 会从旧文件路径兜底解析 `年份 / 试卷类型 / 试卷名称 / 原卷题号 / 知识板块`，避免 summary 字段缺失导致候选池信息不完整；
- 组卷页 `SQLite 试用题库` 下的 AI 按钮由禁用提示改为真实入口；
- 新增 `SQLite 智能预组卷` 面板：
  - 知识板块选择；
  - 动态难度星级；
  - 候选池上限：`500 / 1000 / 全部`；
  - 组卷意图输入；
  - AI 润色想法；
  - 一键开始 SQLite 智能抽题；
- SQLite 智能抽题结果会覆盖当前试题篮，并同步：
  - `exam_selected_qs`；
  - `exam_q_count_input`；
  - `_count_widget`；
  - `exam_expanded_q`；
  - `exam_blocks` 顺序；
- 旧 `CSV + chapters` 的 AI 辅助预组卷也已改为调用同一套 `services/exam_selection_service.py`，减少 SQLite 与旧入口的规则漂移；
- 新增 `scripts/smoke_exam_selection_service.py`；
- 新增 `scripts/smoke_legacy_exam_selection_service.py`；
- `scripts/release_readiness.py` 纳入 `smoke_exam_selection_service` 与 `smoke_legacy_exam_selection_service`。

边界说明：

- SQLite AI 抽题当前仍是“SQLite 作为索引，旧 `.tex` 作为排版载体”的过渡方案；
- SQLite 本身仍只读；
- 抽题结果进入旧试题篮后，后续生成试卷仍沿用旧模板、旧排版、旧 PDF 编译链路；
- 旧 `CSV + chapters` AI 辅助预组卷已切换到共享服务，但默认数据源和 UI 路径仍保留不变；
- 后续如果实机稳定，可以再考虑把旧全局浏览也逐步改成调用同一服务，进一步减少重复逻辑。

验证：

```text
python -m py_compile question_bank_app.py services/exam_selection_service.py services/sqlite_legacy_adapter.py services/question_db_service.py scripts/smoke_exam_selection_service.py
python scripts/smoke_exam_selection_service.py
python scripts/audit_sqlite_legacy_bridge.py --no-report
python scripts/smoke_legacy_exam_selection_service.py
```

结果：

```text
py_compile: ok
smoke_exam_selection_service: status=ok, writes_formal_database=false, writes_legacy_tex=false
regular_selection_target_met: selected=10, candidates=806
paper_selection_nonempty: selected=19, candidates=806
selection_paths_unique: ok
source_database_unchanged: ok
audit_sqlite_legacy_bridge: status=ok, scanned=893, resolved_paths=893, unresolved=0
smoke_legacy_exam_selection_service: status=ok, writes_formal_database=false, writes_legacy_tex=false
```

当前整体进度可从约 `98%` 提升到约 `98.5%`。剩余主要工作：

1. SQLite 试用选题和 SQLite AI 抽题的实机 UI 回归；
2. 旧全局浏览是否需要增加同类“SQLite 试用题卡”模式；
3. 旧 AI 辅助预组卷是否逐步复用 `exam_selection_service`；
4. 最终迁移前的旧题卡/SQLite 题卡行为差异清单；
5. 发布前完整回归与文件清单确认。

### 15.14 2026-09-03 旧 TeX / SQLite 一致性审计与发布检查补强

本轮重点处理最终迁移前最容易遗漏的问题：SQLite 过渡题卡是否与旧 `chapters/` 题源保持一对一关系，以及哪些字段差异属于真正阻塞、哪些只是旧数据口径不一致带来的人工复核项。

已完成：

- 新增 `scripts/audit_legacy_sqlite_consistency.py`；
- 审计脚本只读读取三类来源：
  - 旧 `chapters/` 下真实题目 `.tex`；
  - 本地可重建的 `utils/题库索引表.csv`，若存在则参与对照；
  - `data/mathcyclus.sqlite3` 的 SQLite 题目摘要和旧题卡适配行；
- 旧 TeX 侧不依赖 CSV，而是直接扫描 `chapters/`，跳过 `content_*.tex` 和 `相关图` 辅助 TeX；
- SQLite 侧通过 `services/sqlite_legacy_adapter.py:list_sqlite_legacy_rows()` 读取旧 row 形状，避免加载全部完整题卡和 LaTeX 预览；
- 一致性审计覆盖：
  - SQLite 题目总数；
  - 旧 TeX 题目总数；
  - 路径是否一对一匹配；
  - SQLite 独有路径；
  - 旧 TeX 独有路径；
  - 重复路径；
  - 字段差异分布；
  - 阻塞字段差异样例；
  - 非阻塞字段差异样例；
  - CSV 缓存与旧 TeX 扫描结果差异；
- 强阻塞字段收窄为：
  - `题目ID`；
  - `文件名称`；
  - `相对文件路径`；
- `年份 / 试卷类型 / 试卷名称 / 原卷题号 / 知识板块 / 题型 / 难度星级 / 标签 / 备注` 等字段差异先作为 warning 暴露，不直接阻塞发布；
- 这样处理的原因是当前旧题库里确实存在“文件名、problem 题头、CSV 缓存、SQLite 归一化字段”不完全一致的历史数据：
  - 例如多知识板块题目在旧 TeX 中写作 `函数，导数`，SQLite 当前主板块会归为 `函数`；
  - 部分 2026 模拟题的文件名与题头来源信息存在历史不一致；
  - 部分题型在旧 CSV 规则和 SQLite 推断规则下存在差异；
- 这些差异需要进入后续数据治理清单，但不影响目前“SQLite 作为索引、旧 TeX 作为排版载体”的过渡链路；
- `scripts/release_readiness.py` 已纳入 `audit_legacy_sqlite_consistency`：
  - 输出 `status=warning` 时作为发布前人工复核项；
  - 输出 `status=blocked` 时阻断发布检查；
  - 与 `audit_sqlite_legacy_bridge` 形成互补：前者看字段一致性，后者看旧路径可解析与题卡结构。

本轮审计结果：

```text
audit_legacy_sqlite_consistency:
status=warning
sqlite_rows=893
legacy_tex_rows=893
tex_only=0
sqlite_only=0
field_mismatches=57
blocking_mismatches=0
```

解释：

- `SQLite` 与旧 `chapters/` 题源已经达到 `893 / 893` 的路径级覆盖；
- 没有 SQLite 独有题，也没有旧 TeX 独有题；
- 没有 `题目ID / 文件名称 / 相对文件路径` 层面的阻塞差异；
- 57 个字段差异属于历史元数据口径问题，后续应作为数据治理任务逐步处理，不应在本阶段直接修改旧题文件；
- 本轮没有写入正式 SQLite，没有覆盖旧 `.tex`，没有写入 CSV 索引。

文件管理约束：

- `reports/` 下新生成的审计报告仍属于本地生成物，已被 `.gitignore` 忽略；
- `data/mathcyclus.sqlite3` 仍是本地数据库，不直接提交；
- `scripts/audit_legacy_sqlite_consistency.py`、`scripts/release_readiness.py`、`services/*.py`、`db/schema.sql`、`templates/`、规划文档属于本轮重构应提交的源码/文档资产；
- 不删除旧 `chapters/` 题源，不移动题目文件，不重命名图片资源；
- `utils/题库索引表.csv` 是可重建缓存，缺失不阻塞，但本地 UI 使用前可以通过工具箱重建；
- 后续若要做仓库瘦身，应只处理明确已忽略的生成物、缓存、历史备份，不处理当前重构源码。

完整发布回归：

```text
python -m py_compile question_bank_app.py services/exam_selection_service.py services/sqlite_legacy_adapter.py services/question_db_service.py scripts/audit_legacy_sqlite_consistency.py scripts/release_readiness.py
python scripts/audit_legacy_sqlite_consistency.py --no-report
python scripts/release_readiness.py
```

结果：

```text
py_compile: ok
audit_legacy_sqlite_consistency: status=warning, sqlite_rows=893, legacy_tex_rows=893, tex_only=0, sqlite_only=0, field_mismatches=57, blocking_mismatches=0
release_readiness: status=warning, failed=0, warnings=2
```

`release_readiness` 的 2 个 warning 均不是失败：

1. `project_hygiene`：提示当前工作区存在真实源码/文档改动，提交前需要人工确认；
2. `audit_legacy_sqlite_consistency`：提示 57 个非阻塞旧元数据差异，路径与身份字段没有阻塞差异。

Git 文件清单复核：

- 应提交：新增 `services/` 重构服务、`scripts/` 审计/迁移/smoke 工具、`db/schema.sql`、`db/seed/` 人工复核种子、`docs/` 规划日志、`templates/` 示例、`.gitkeep` 占位文件、`.gitignore`、`question_bank_app.py`、`题库数据库重构实施规划.md`、`题库重构参考沉淀.md`；
- 不应提交：`data/*.sqlite3`、`reports/*`、`exports/*`、`assets/questions/*`、`utils/题库索引表.csv`、`__pycache__/`、`.tikz_cache/`、`.venv/`；
- 当前 `.gitignore` 对数据库、报告、导出、题目图片目录和 CSV 缓存均覆盖正常；
- `启动程序.py` 在 Git 状态中可能出现中文路径/stat cache 修改提示，但项目卫生报告显示真实内容 diff 主要是 `.gitignore` 和 `question_bank_app.py`，提交前仍建议用 `git diff -- 启动程序.py` 再确认一次。

下一步顺序：

1. 用户在本机实机打开 SQLite 试用选题与 AI 抽题入口，确认 UI 手感；
2. 若 UI 无异常，可准备 Git 提交与推送；
3. 后续另起数据治理任务，处理 57 个非阻塞字段差异；
4. 等试用入口稳定后，再决定是否把 SQLite 从“试用入口”切成“默认入口”。

当前整体进度可从约 `98.5%` 提升到约 `99%`。剩余主要工作：

1. SQLite 试用选题和 SQLite AI 抽题的实机 UI 人工确认；
2. Git 提交前人工复核文件清单；
3. 旧元数据差异治理清单；
4. 最终决定何时从“SQLite 试用入口”切到“SQLite 默认入口”。

### 15.15 2026-09-03 图片资产、安装包与本地数据边界

本轮根据后续“方便他人安装/配置/迁移，但个人本地题库数据不上传 GitHub”的目标，明确图片与数据文件管理原则。

图片资产方案确认：

- 非 TikZ 图片不直接写进 `.tex` 源码，也不把图片二进制塞入 SQLite；
- 图片实体文件放在项目内的 `assets/questions/<question_id>/`；
- SQLite 通过 `question_asset` 表记录每张图片和题目的关系；
- 每条 `question_asset` 至少记录：
  - `question_id`：绑定到哪一道题；
  - `role`：用于题干、答案、解析或附件；
  - `file_path`：项目相对路径；
  - `original_file_name`：原始文件名；
  - `mime_type`：文件类型；
  - `width / height`：图片尺寸；
  - `file_hash`：文件内容校验；
  - `caption`：人工说明或引用名；
  - `sort_order`：同一用途下的排序；
- TeX 中通过 `\questionasset{引用名}` 占位，预览和导出时再由数据库解析到真实图片；
- 多图最终显示顺序以 TeX 中多个 `\questionasset{...}` 的出现顺序为准，资源列表顺序只用于管理视图和默认建议。

安装包 / 迁移目标：

- GitHub 仓库应提供“程序、schema、脚本、模板、说明文档”，不提供个人本地题库数据；
- 个人本地数据包括：
  - `data/mathcyclus.sqlite3`；
  - `data/backups/*.sqlite3`；
  - `reports/*`；
  - `exports/*`；
  - `assets/questions/*`；
  - `utils/题库索引表.csv`；
  - 旧题源 `.tex` 文件；
- 当前 `.gitignore` 已把上述本地数据和生成物排除，后续新增目录时必须同步检查；
- 未来安装包应支持“空库初始化”：
  - 首次启动时根据 `db/schema.sql` 创建空 SQLite；
  - 自动创建 `data/`、`assets/questions/`、`exports/`、`reports/` 等本地目录；
  - 使用 `templates/` 中的示例说明导入格式；
- 未来迁移包应支持“用户自己的题库搬家”：
  - 导出 SQLite 数据库；
  - 导出 `assets/questions/` 图片资源；
  - 可选导出旧 `.tex` 题源；
  - 通过相对路径保持跨电脑可迁移；
  - 迁移后运行审计脚本确认图片引用、题目来源、旧 TeX 桥接是否完整；
- 发布到 GitHub 前不应把个人真实数据库、题目图片、生成报告、导出试卷纳入 commit；
- 后续若要做正式安装包，应新增 `scripts/init_local_workspace.py` 或同类初始化脚本，专门负责本地目录和空库创建，避免用户手动补目录。

本轮 UI 小清理：

- 录入问题页右侧 `question_import_draft` 技术 badge 已移除；
- 草稿表说明仍保留在页面文案和规范说明中，避免普通用户看到内部表名造成困惑。

### 15.16 2026-09-03 本地初始化与迁移包工具预部署

本轮提前为未来 1.0 版本做“代码可公开、数据留本地、用户可迁移”的基础设施。当前只新增独立脚本和文档，不改录入、浏览、组卷等主业务逻辑。

新增脚本：

- `scripts/init_local_workspace.py`
- `scripts/local_data_bundle.py`
- `scripts/smoke_local_workspace_tools.py`

`scripts/init_local_workspace.py` 的作用：

- 面向新用户 clone / 下载项目后的首次本地初始化；
- 自动创建本地运行目录：
  - `data/`
  - `data/backups/`
  - `data/indexes/`
  - `assets/`
  - `assets/questions/`
  - `exports/`
  - `reports/`
- 自动补齐这些目录下的 `.gitkeep`；
- 当 `data/mathcyclus.sqlite3` 不存在时，按 `db/schema.sql` 创建空 SQLite 数据库；
- 若数据库已存在，只读取表结构，不覆盖、不重建、不删除；
- 可通过 `--dry-run` 预览动作；
- 可通过 `--strict-gitignore` 检查个人数据路径是否被 Git 忽略。

`scripts/local_data_bundle.py` 的作用：

- 面向用户个人数据备份和换电脑迁移；
- 支持三个子命令：
  - `export`：导出迁移包；
  - `inspect`：检查迁移包清单；
  - `restore`：恢复迁移包；
- 默认导出到 `data/backups/mathcyclus_local_bundle_*.zip`，该目录已被 `.gitignore` 忽略；
- 默认包含：
  - `data/mathcyclus.sqlite3`
  - `assets/questions/`
  - `utils/题库索引表.csv`
- 默认不包含：
  - `chapters/`
  - `reports/`
  - `exports/`
- 如需迁移旧 `.tex` 题源，可显式使用 `--include-legacy-tex`；
- 迁移包内带 `mathcyclus_local_bundle_manifest.json`，记录文件数量、大小、hash、是否包含个人数据、是否适合 Git 等信息；
- 恢复默认 dry-run，不写文件；
- 只有加 `restore --apply` 才真正写入；
- 默认不覆盖已有文件，只有显式加 `--overwrite` 才允许覆盖；
- 恢复时拒绝路径穿越，只允许写入受控路径；
- 恢复过程不删除任何文件。

`scripts/smoke_local_workspace_tools.py` 的作用：

- 用临时目录模拟新项目；
- 复制 `db/schema.sql`；
- 验证初始化脚本能创建空库；
- 验证 `question_type` 基础种子存在；
- 伪造一张题目图片和一个 CSV 缓存；
- 验证迁移包导出、检查、dry-run 恢复、实际恢复；
- 验证再次恢复时会正确报告冲突；
- 不写当前项目真实 `data/`、`assets/questions/` 或旧题源。

发布检查接入：

- `scripts/release_readiness.py` 已加入 `smoke_local_workspace_tools`；
- 后续每次发布前都会自动检查本地初始化和迁移包工具没有退化；
- 这个 smoke 只操作临时目录，不会污染当前项目数据。

新增说明文档：

- `docs/planning/local_workspace_and_migration.md`

文档覆盖：

- 哪些内容不应进入 GitHub；
- 新用户如何初始化本地目录和空库；
- 老用户如何导出迁移包；
- 如何检查迁移包；
- 如何恢复迁移包；
- 图片资产如何随数据库一起迁移；
- 1.0 发布前建议流程；
- 后续可扩展到图形化首次启动向导和设置页备份/恢复按钮。

当前边界：

- 这一步不是正式安装包；
- 还没有把初始化入口接入 `启动程序.py` 或 Streamlit 启动流程；
- 还没有在前端设置页做“备份 / 恢复”按钮；
- 还没有做 schema migrations 的版本升级机制；
- 当前先把底层 CLI 工具做好，等录入、组卷和 SQLite 默认入口进一步稳定后，再接图形化入口。

推荐未来 1.0 发布流程：

```text
python scripts/init_local_workspace.py --dry-run --strict-gitignore
python scripts/smoke_local_workspace_tools.py
python scripts/release_readiness.py
```

用户首次本地使用流程：

```text
python scripts/init_local_workspace.py
streamlit run question_bank_app.py
```

用户个人迁移流程：

```text
python scripts/local_data_bundle.py export --include-legacy-tex
python scripts/local_data_bundle.py inspect data/backups/mathcyclus_local_bundle_YYYYMMDD_HHMMSS.zip
python scripts/local_data_bundle.py restore data/backups/mathcyclus_local_bundle_YYYYMMDD_HHMMSS.zip
python scripts/local_data_bundle.py restore data/backups/mathcyclus_local_bundle_YYYYMMDD_HHMMSS.zip --apply
```

当前整体进度仍按约 `99%` 计算。剩余主要工作：

1. 前端实机确认；
2. 录入和组卷继续调整；
3. Git 提交前文件清单人工确认；
4. 未来决定是否把初始化/迁移工具接入图形化界面或启动器。

本轮验证：

```text
python -m py_compile scripts/init_local_workspace.py scripts/local_data_bundle.py scripts/smoke_local_workspace_tools.py scripts/release_readiness.py
python scripts/init_local_workspace.py --dry-run --strict-gitignore
python scripts/local_data_bundle.py export --dry-run
python scripts/smoke_local_workspace_tools.py
python scripts/release_readiness.py --skip-slow
python scripts/release_readiness.py
```

结果：

```text
init_local_workspace: status=ok, database_status=exists, database_tables=21, gitignore_ok=True
local_data_bundle export --dry-run: status=ok, item_count=3, contains_personal_data=True, intended_for_git=False
smoke_local_workspace_tools: status=ok
release_readiness --skip-slow: status=warning, failed=0, warnings=1
release_readiness: status=warning, failed=0, warnings=2
```

说明：

- 新增工具通过编译；
- 初始化脚本不会覆盖已有数据库；
- 迁移包 dry-run 明确标注包含个人数据且不适合 Git；
- 迁移 smoke 只写临时目录，不写当前项目数据；
- 完整发布检查无失败，两个 warning 仍是“工作区有真实改动”和“旧元数据 57 个非阻塞差异”。

## 15.17 2026-09-03 SQLite 预览题卡资料快速编辑

本轮针对“能不能在 SQLite 数据库题目预览中直接设置难度、标签或备注”的需求，采用轻量化题卡资料编辑方案，而不是让用户每次进入完整 TeX 编辑工作台。

实现原则：

- 在 SQLite 题目预览卡片顶部元信息行新增 `标签信息` 入口，弹层标题仍为 `修改标签信息`；
- 入口使用轻量弹出层，只包含 `难度星级`、`标签`、`备注` 三类字段；
- 该入口只修改结构化 SQLite 数据，不修改旧 `.tex` 源文件；
- 保存时通过 `services/question_edit_service.py` 的统一写入服务进入数据库；
- 若资料确实发生变化，同步刷新 `canonical_tex` 里的 Label Data 元数据；
- 每次有效保存都会写入 `question_revision`，便于未来追溯；
- 保存后清理 SQLite 预览题目 payload 缓存，避免题卡仍显示旧资料；
- 若没有检测到变化，只给出提示，不写 revision。

当前边界：

- 难度仍沿用现有 schema 与服务层约束，只支持 `1-5` 整数星级或未设置；
- 暂不引入半星、6 星或更多维度，否则需要同步调整 schema、迁移脚本、导出逻辑和旧题兼容策略；
- 快速资料编辑不替代完整 TeX 编辑，题干、选项、答案、解析仍应进入单题 TeX 编辑工作台修改；
- 旧 `.tex` 文件仍保持只读兼容层定位，SQLite 预览里的资料修改不会反写旧文件。

对应验证：

```text
python -m py_compile question_bank_app.py scripts/smoke_question_edit_service.py
python scripts/smoke_question_edit_service.py
```

`scripts/smoke_question_edit_service.py` 增加了 `metadata_update_persisted` 检查，用于确认备注等资料能写入 SQLite，并同步出现在 `canonical_tex` 中。

## 15.18 2026-09-03 SQLite 独立预览页题卡操作精简

在实机预览后，对独立 `SQLite 数据库预览` 页面做进一步收敛：该页面定位为“浏览、核对、进入本题编辑”，不再承担组卷选题和单题下载 TeX 的任务。

本轮调整：

- 独立 SQLite 预览入口调用 `render_sqlite_readonly_browse_preview(allow_exam_basket=False)`；
- 独立预览页左侧不再显示试题篮计数、加入当前页、随机补足等组卷操作；
- 题卡顶部不再显示“开始修改 TeX / 加入或移出试题篮 / 下载 TeX”这一整行按钮；
- 题卡顶部元信息行保留 `标签信息`，用于快速修改 `难度`、`标签`、`备注`；
- `TeX 源码` 标题下方不再重复展示 `难度`、`标签`、`备注`；
- `TeX 源码` 和源码框之间只保留一个入口：`开始修改本题信息`；
- 点击 `开始修改本题信息` 后仍进入原有单题编辑工作台，可修改题干、选项、答案、解析、难度、标签、备注和图片/来源关系；
- 组卷服务里的 SQLite 试用选题仍保留 `allow_exam_basket=True`，不影响组卷流程。

取舍说明：

- `标签信息` 只服务轻量资料修改，完整题干、选项、答案、解析仍进入 `开始修改本题信息`；
- 原因是题卡按钮过多会削弱“源码 + 预览”的主任务，因此只保留一个轻量资料入口和一个完整编辑入口；
- `下载 TeX` 从单题预览卡移除，但批量/来源级 TeX 导出仍保留在专门导出入口中；
- SQLite 编辑仍只写结构化数据库和 `canonical_tex`，不反写旧 `.tex` 文件。

## 15.19 2026-09-03 旧 TeX 迁移入口与数据统计改造计划

本轮确认两个后续必须纳入 1.0 路线的兼容问题：数据统计页的数据来源，以及早期用户从旧本地 TeX 题库升级到 SQLite 的迁移入口。

### 数据统计页后续改造

当前 `数据统计` 仍主要依赖旧体系：

- 优先读取 `utils/题库索引表.csv`；
- CSV 不可用时降级扫描 `chapters/`；
- 因此 SQLite 预览页中直接修改的难度、标签、备注等结构化信息，不一定会立即进入统计口径；
- 这在过渡期可以接受，但不适合作为 1.0 后的最终状态。

后续目标：

- `数据统计` 改为 SQLite 优先；
- 当 `data/mathcyclus.sqlite3` 存在且结构完整时，统计直接读取数据库；
- 当 SQLite 不存在时，才回退到 CSV / `chapters/`；
- 页面上应显示统计来源，例如 `SQLite 正式库`、`CSV 索引缓存`、`旧 TeX 扫描兜底`；
- 难度分布、标签词云、题型分布、知识板块分布应读取 SQLite 结构化字段；
- 今日新增、今日修改应优先使用数据库 `created_at / updated_at` 和 `question_revision`；
- TikZ / 图片统计需要从 `question_asset` 与旧 TeX 扫描结果合并过渡，最终转为资源表统计；
- 统计服务建议拆到 `services/statistics_service.py`，避免继续在 `question_bank_app.py` 中堆业务逻辑。

第一步实现建议：

```text
services/statistics_service.py
  get_statistics_sqlite_first()
  get_statistics_from_sqlite()
  get_statistics_from_csv()
  get_statistics_from_legacy_tex()
```

页面层只调用一个统一入口，并展示数据来源 badge。这样未来切换主库时，不需要再改统计页布局。

### 旧 TeX 到 SQLite 迁移工具

为了兼容已经部署早期版本的用户，工具箱需要保留一个图形化迁移入口。

本轮已在 `工具箱` 新增：

- `12. 旧 TeX 迁移到 SQLite`；
- 入口名称：`进入旧库迁移工具`；
- 子页面：`旧 TeX 题库迁移到 SQLite`。

迁移工具当前坚持安全优先：

- 第一步只调用 `scripts/migrate_tex_to_db_dry_run.py`；
- 只生成预览库 `data/mathcyclus_preview_*.sqlite3`；
- 只生成报告 `reports/tex_to_db_dry_run_*.md`；
- 不修改旧 `.tex`；
- 不写入正式 `data/mathcyclus.sqlite3`；
- 支持填写旧 `chapters` 目录；
- 支持 `limit` 小样本迁移，方便先排查历史格式问题；
- 支持查看脚本输出和报告预览。

第二步只做提升检查：

- 调用 `scripts/promote_preview_to_database.py` 的默认 dry-run 模式；
- 生成表级差异、阻塞项、warning 和样例 ID；
- 页面内不提供一键正式覆盖按钮；
- 正式写入仍需命令行显式确认：

```text
python scripts/promote_preview_to_database.py --apply --confirm PROMOTE_SQLITE_PREVIEW
```

这样设计的原因：

- 迁移是高风险动作；
- 用户可能已经有正式 SQLite 数据库；
- 误覆盖会比普通 UI 排版问题严重得多；
- 图形界面先做 dry-run 和报告查看，正式写入保留人工强确认，更适合 1.0 前的过渡期。

### 迁移工具未来增强

后续可以继续增加：

- 迁移前自动检查旧目录中问题数量、异常文件、图片引用缺失；
- 迁移后对比旧 TeX、CSV、SQLite 的路径覆盖率；
- 预览库题卡快速浏览入口；
- 对 warning 按类型分组，例如题头缺失、选项异常、图片引用缺失、题型推断异常；
- 正式写入前要求用户输入确认文本；
- 写入后自动运行 `scripts/audit_legacy_sqlite_consistency.py`；
- 支持旧版本用户从迁移包恢复本地 `data/` 和 `assets/questions/`。

### API 设置边界

API 设置不需要跟随 SQLite 重构同步改造。它的职责仍然是：

- 读取和保存 `.env` 中的模型/API 配置；
- 读取和保存 `ocr_prompt.txt`；
- 服务 AI 识别、AI 修改、语义索引等调用。

因此：

- `API 设置` 不应读取题库数据库；
- `API 设置` 不应承担迁移状态展示；
- 与数据库迁移相关的信息应放在工具箱、规范说明和迁移报告中。

当前整体进度仍按约 `99%` 计算。剩余主要工作：

1. 数据统计服务 SQLite-first 改造；
2. 旧 TeX 迁移工具实机试跑；
3. 迁移报告和提升检查报告的文案微调；
4. 录入、组卷、SQLite 预览继续做最终交互回归；
5. 1.0 前文件清单、忽略规则、发布说明统一检查。

## 15.20 2026-09-03 SQLite 题卡按钮修正与版本更新助手

本轮处理 SQLite 数据库预览题卡的两个细节，并提前部署未来 GitHub / 安装包更新流程的基础脚本。

### SQLite 预览题卡 UI 修正

调整点：

- `开始修改本题信息` 从 `TeX 源码` 标题上方移回标题下一行；
- `标签信息` 弹出按钮改为更窄的小胶囊按钮；
- 限制按钮容器宽度，避免按钮文字被挤成竖排；
- 按钮字号调小，减少对题目信息行的干扰；
- 改动只作用于 SQLite 数据库预览题卡，不影响旧全局浏览、组卷服务和录入新题。

设计取舍：

- `标签信息` 是轻量编辑入口，应保持小而明确；
- 完整题目内容修改仍走 `开始修改本题信息`；
- 题卡主视觉仍优先给 `TeX 源码` 与右侧渲染预览，不让元信息按钮喧宾夺主。

### 版本更新助手

新增：

- `scripts/update_local_installation.py`
- `docs/planning/version_update_and_installer.md`

`scripts/update_local_installation.py` 的定位：

- 面向从 GitHub 下载项目的普通用户；
- 用于后续每次版本更新时统一执行“检查、备份、拉取、安装依赖、补齐本地目录、快速检查”；
- 默认 dry-run，不实际修改；
- 真实执行必须加 `--apply`；
- 不删除本地数据；
- 不覆盖正式 SQLite 数据库；
- 不上传 `.env`、数据库、图片、报告、导出文件；
- 如果工作区有未提交改动，默认拒绝 `git pull`，避免冲突。

典型预览命令：

```text
python scripts/update_local_installation.py --pull --install-deps --run-checks
```

典型执行命令：

```text
python scripts/update_local_installation.py --apply --pull --install-deps --run-checks
```

如果早期用户仍在使用旧 `chapters/` TeX 题源，更新前需要一起备份：

```text
python scripts/update_local_installation.py --apply --pull --install-deps --run-checks --include-legacy-tex-backup
```

当前边界：

- 这还不是完整安装包；
- 还没有 GUI 内“检查更新”按钮；
- 还没有 schema migrations；
- 后续 1.0 前应补 `db/migrations/` 和数据库版本号表；
- 等核心功能稳定后，再把更新流程写进 `README.md`。

后续部署方向：

1. GitHub 源码安装：`git clone` + `.venv` + 初始化脚本 + 更新助手；
2. 图形化启动器：给普通用户双击启动、检查更新、备份恢复；
3. 打包版：程序目录和用户数据目录分离，支持跨电脑迁移。

当前整体进度仍按约 `99%` 计算。剩余主要工作：

1. 数据统计服务 SQLite-first；
2. schema version 与 migrations；
3. 迁移入口实机确认；
4. README 安装/升级说明；
5. 最终发布文件清单检查。

## 15.21 2026-09-03 数据统计 SQLite-first 改造

本轮完成 `数据统计` 的数据来源重构：统计页不再直接把 CSV 作为唯一主来源，而是统一走服务层，并按 `SQLite → CSV → 旧 TeX 扫描` 的顺序兜底。

新增：

- `services/statistics_service.py`
- `scripts/smoke_statistics_service.py`

调整：

- `question_bank_app.py` 中的 `get_statistics()` 改为调用 `services.statistics_service.get_statistics_sqlite_first()`；
- 旧 CSV 统计逻辑迁移到服务层作为 fallback；
- 旧 `chapters/` 扫描统计逻辑迁移到服务层作为最后兜底；
- `数据统计` 页面新增轻量数据来源标识，显示当前读取的是：
  - `SQLite 正式库`；
  - `CSV 索引缓存`；
  - `旧 TeX 扫描兜底`；
- `scripts/release_readiness.py --skip-slow` 已接入 `smoke_statistics_service`，防止后续统计服务退化。

SQLite-first 口径：

- 总题数读取 `question`；
- 题型分布读取 `question_type.name`；
- 知识板块优先读取主 `question_knowledge_area` / `knowledge_area`，没有时回退 `legacy_question_map.detected_chapter`；
- 难度分布读取 `question.difficulty`；
- 标签词云读取 `question.tags_json`；
- TikZ 总数通过题目 TeX 字段中是否包含 `\begin{tikzpicture}` 判断；
- 今日新增优先读取 `question.created_at`；
- 今日修改优先读取 `question_revision.created_at`，没有修订记录时回退 `question.updated_at`；
- `WK` 挖空题继续从普通统计中排除，避免影响原本题库总量口径。

边界说明：

- 当前 `difficulty_count=0` 可能是因为正式 SQLite 中大量旧题尚未设置结构化难度，不代表统计服务失败；
- SQLite 统计不会写库；
- CSV fallback 仍沿用 `utils.local_stats.sync_question_activity()` 的本地快照机制；
- 旧 TeX fallback 只在 SQLite 和 CSV 都不可用时启用。

验证：

```text
python -m py_compile question_bank_app.py services/statistics_service.py scripts/smoke_statistics_service.py scripts/release_readiness.py
python scripts/smoke_statistics_service.py
python scripts/release_readiness.py --skip-slow
node C:\Users\pc\.agents\skills\impeccable\scripts\detect.mjs --json question_bank_app.py
```

结果：

```text
py_compile: ok
smoke_statistics_service: status=ok
release_readiness --skip-slow: status=warning, failed=0, warnings=1
impeccable detect: []
```

`release_readiness` 的 warning 仍是工作区存在未提交改动，不是统计服务失败。

当前整体进度可从约 `99%` 提升到约 `99.2%`。剩余主要工作：

1. schema version 与 migrations；
2. 旧 TeX 迁移工具实机确认；
3. README 安装/升级说明；
4. SQLite 默认入口切换前的最终回归；
5. Git 提交前文件清单确认。

## 15.22 2026-09-03 Schema version 与数据库迁移机制

本轮完成 SQLite schema 版本管理基础设施。目标是后续用户从 GitHub 更新项目后，可以安全地把自己的本地数据库升级到新结构，而不是手工改库或重建库。

新增：

- `db/migrations/0001_schema_version_baseline.sql`
- `services/schema_migration_service.py`
- `scripts/migrate_schema.py`
- `scripts/smoke_schema_migration_service.py`
- `docs/planning/schema_migration_flow.md`

调整：

- `db/schema.sql` 新增 `app_meta` 表；
- `db/schema.sql` 新增 `schema_migration` 表；
- 新建空库默认写入：
  - `app_name = MathCyclus`
  - `schema_version = 1`
  - `schema_baseline = 20260903`
  - `schema_migration` 基线记录；
- `scripts/update_local_installation.py` 已接入 `scripts/migrate_schema.py`；
- `scripts/release_readiness.py --skip-slow` 已接入 `smoke_schema_migration_service`；
- `docs/planning/version_update_and_installer.md` 和 `docs/planning/local_workspace_and_migration.md` 已同步更新。

迁移机制：

- 迁移 SQL 存放在 `db/migrations/`；
- 文件名格式为 `0001_xxx.sql`、`0002_xxx.sql`；
- `scripts/migrate_schema.py` 默认只 dry-run；
- 真正应用迁移必须加 `--apply`；
- 应用前默认把 SQLite 备份到 `data/backups/`；
- 不删除文件；
- 不修改旧 `.tex`；
- 不移动 `assets/questions/`；
- 如果已应用迁移的 checksum 与当前迁移文件不一致，拒绝继续。

常用命令：

```text
python scripts/migrate_schema.py --status-only
python scripts/migrate_schema.py
python scripts/migrate_schema.py --apply
```

当前正式库状态：

```text
status=pending
database=data/mathcyclus.sqlite3
current_version=0
target_version=1
pending_count=1
applied_count=0
```

解释：

- 你的正式库是之前创建的，因此还没有 `app_meta` / `schema_migration` 版本记录；
- 当前只有 `0001_schema_version_baseline.sql` 这一条待应用；
- 该迁移只补版本元数据，不改题目内容；
- 本轮没有自动对正式库执行 `--apply`，避免在未实机确认前写入你的本地数据库。

验证：

```text
python -m py_compile services/schema_migration_service.py scripts/migrate_schema.py scripts/smoke_schema_migration_service.py scripts/update_local_installation.py scripts/release_readiness.py
python scripts/smoke_schema_migration_service.py
python scripts/migrate_schema.py --status-only
```

结果：

```text
py_compile: ok
smoke_schema_migration_service: status=ok
migrate_schema --status-only: status=pending, current_version=0, target_version=1, pending_count=1
```

当前整体进度可从约 `99.2%` 提升到约 `99.35%`。剩余主要工作：

1. 用户确认后可对正式库执行 `python scripts/migrate_schema.py --apply`；
2. 旧 TeX 迁移工具实机确认；
3. README 安装/升级说明；
4. SQLite 默认入口切换前最终回归；
5. Git 提交前文件清单确认。

## 15.23 2026-09-03 README 安装、升级与本地数据边界同步

本轮把前面完成的本地初始化、版本更新、schema migration 和 SQLite 数据边界写入 `README.md`，避免后续 GitHub 用户只看到旧 CSV / TeX 工作流说明。

已更新：

- `README.md`

主要补充：

- 系统说明从“CSV 元数据索引”更新为“兼容旧 CSV，并逐步升级为 SQLite 结构化题库”；
- 核心功能补充 SQLite 已支持：
  - 题目本体；
  - 来源关系；
  - 图片资源；
  - 修订记录；
  - TeX 导出；
- 技术栈补充 SQLite；
- 快速启动流程新增：

```text
python scripts/init_local_workspace.py
```

- 新增“本地升级”章节，给出源码版用户更新命令：

```text
python scripts/update_local_installation.py --pull --install-deps --run-checks
python scripts/update_local_installation.py --apply --pull --install-deps --run-checks
```

- 新增 schema migration 检查和应用命令：

```text
python scripts/migrate_schema.py --status-only
python scripts/migrate_schema.py
python scripts/migrate_schema.py --apply
```

- 核心目录结构补充：
  - `db/`
  - `services/`
  - `scripts/`
  - `data/`
  - `assets/questions/`
  - `reports/`
  - `exports/`
- 仓库维护说明补充：
  - `data/mathcyclus.sqlite3` 不提交；
  - `assets/questions/` 不提交；
  - `reports/`、`exports/` 不提交；
  - `db/schema.sql` 和 `db/migrations/` 需要提交；
  - `scripts/migrate_schema.py` 默认 dry-run。

当前整体进度可从约 `99.35%` 提升到约 `99.45%`。剩余主要工作：

1. 用户确认后应用正式库基线迁移；
2. 旧 TeX 迁移工具实机确认；
3. SQLite 默认入口切换前最终回归；
4. Git 提交前文件清单确认；
5. 后续再考虑图形化“检查更新 / 数据库升级”入口。

## 15.24 2026-09-03 正式库基线迁移、旧库迁移入口验证与核心回归

本轮执行前面剩余清单中的前三项：正式库 schema 基线迁移、旧 TeX 迁移工具实机小样本验证、SQLite 核心回归检查。原则仍然是“只升级结构元数据，不改题目内容；只验证旧库迁移链路，不覆盖正式库；只清理本轮临时发布检查报告，不删除个人数据库和历史验证资产”。

### 正式库 schema 基线迁移

已执行：

```text
python scripts/migrate_schema.py --apply --stamp 20260903_baseline_apply
```

结果：

```text
status=ok
database=data/mathcyclus.sqlite3
current_version=1
target_version=1
pending_count=0
applied_count=1
```

本次迁移影响范围：

- 向正式 SQLite 补齐 `app_meta` / `schema_migration` 版本记录；
- 记录当前 schema version 为 `1`；
- 不修改 `question`、`paper`、`paper_question` 等题目业务表内容；
- 不修改旧 `.tex` 题库；
- 不移动或删除图片资源。

自动备份：

```text
data/backups/mathcyclus_schema_migration_20260903_baseline_apply.sqlite3
```

该备份属于本地个人数据，继续由 `.gitignore` 忽略，不上传 GitHub。

### 旧 TeX 迁移工具小样本验证

已执行 20 题小样本 dry-run：

```text
python scripts/migrate_tex_to_db_dry_run.py --stamp 20260903_legacy_tool_check_20 --limit 20
python scripts/promote_preview_to_database.py --source-db data/mathcyclus_preview_20260903_legacy_tool_check_20.sqlite3 --stamp 20260903_legacy_tool_check_20
```

结果：

```text
migrate_tex_to_db_dry_run: questions=20, warnings=0
promote_preview_to_database: can_apply=True, written=False, blockers=0, warnings=0
```

判断：

- 旧 `.tex` 到 SQLite 预览库的解析链路可用；
- 旧 ID、旧文件路径、试卷来源、题号等核心字段能进入预览库；
- 提升检查能正确识别“可写入”状态；
- 本轮没有把 20 题样本覆盖写入正式库；
- UI 中的“旧 TeX 迁移到 SQLite”入口仍应默认 dry-run，正式覆盖必须二次确认。

对应本地验证产物：

```text
data/mathcyclus_preview_20260903_legacy_tool_check_20.sqlite3
reports/tex_to_db_dry_run_20260903_legacy_tool_check_20.md
reports/promote_preview_to_database_20260903_legacy_tool_check_20.md
reports/promote_preview_to_database_20260903_legacy_tool_check_20.json
```

这些产物均属于本地验证数据，继续被 `.gitignore` 忽略。

### SQLite 核心回归

已完成的核心检查：

```text
python scripts/release_readiness.py --skip-slow
```

结果：

```text
release_readiness --skip-slow: status=warning, failed=0, warnings=1
```

具体通过项：

- `python_compile`：通过；
- `rebuild_status_brief`：通过；
- `precommit_database_audit`：通过；
- `question_asset_audit`：通过；
- `smoke_statistics_service`：通过；
- `smoke_schema_migration_service`：通过；
- `smoke_local_workspace_tools`：通过。

唯一 warning：

- `project_hygiene` 提示当前工作区存在真实源码/文档改动，需要提交前人工确认；
- 这不是运行失败，也不是数据库错误；
- 本轮生成的 `release_readiness_20260903_161144`、`project_hygiene_20260903_161146`、`precommit_database_audit_20260903_161144`、`question_asset_audit_20260903_161145` 临时报表已清理，避免污染本地 `reports/`。

### 当前进度判断

当前整体数据库重构进度可从约 `99.45%` 提升到约 `99.6%`。

已经完成的主线：

- SQLite 正式库已经存在并通过完整性检查；
- 题目、试卷、来源关系、教材关系、专题关系、图片资源、修订记录、草稿录入等核心表已经落地；
- SQLite 预览、单题编辑、标签信息修改、来源关系、图片资源、TeX 导出、本地初始化、本地迁移包、schema migration、数据统计服务均已形成可验证链路；
- 旧 TeX 到 SQLite 的迁移入口已经具备安全 dry-run 能力；
- GitHub 公开仓库与本地个人数据的边界已经写入 `.gitignore` 和 `README.md`。

剩余主要工作：

1. 提交前人工确认 Git 文件清单，避免误提交本地数据或临时产物；
2. 后续考虑把“检查更新 / 数据库升级 / 旧库迁移”接入图形化入口；
3. 继续按你的后续想法优化“录入新题”的 UI 与批量录入流程；
4. 后续再决定是否把全局浏览、组卷服务彻底切换为 SQLite 默认数据源。
